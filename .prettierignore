# Build artifacts
dist/
build/
out/
coverage/

# Dependencies
node_modules/

# Editors
.vscode/
.idea/
*.swp

# OS / env
.DS_Store
.env.local
.env.*.local

# Minified bundles
*.min.js
*.min.css

# Monorepo outputs
packages/*/dist/

# Ledger de Percepções em Trânsito: a forma é OWNED pelo serializer do domínio
# (insightsLedgerSerializer) e gateada por `insights:check` (forma canônica).
# Deixar o prettier reformatar quebraria a paridade raw↔canônica.
.governance/runtime/insights.yml

# Manifesto compilado de constraints (CO-3.2): forma OWNED pelo serializer
# canônico (constraintManifest) e gateada por `knowledge:check` (existência/
# classe/sync). Prettier reformatar quebraria a paridade raw↔canônica.
.governance/runtime/constraints/manifest.json

# Artefatos de ferramentas de IA (aider, etc.) — scratch local, fora de versão e
# format (CORE-05: não versionar stray context; GR-0202: excluir do contexto).
.aider*

# Projeção PUBLICADA da sim (context.json): a forma é OWNED pelo `build.ts` (deriveContext) e a paridade é
# gateada por `freshness.ts` (compara o DADO, não os bytes). Prettier reformatar criaria diff falso vs o build.
.governance/specs/0024-context-architecture/work-graph-model/_org-simulation-v2/**/.governance/context.json

# Artefatos GERADOS/CACHE da sim (gitignored, derivados por build.ts/viewer): read-models, dashboards e
# snapshots. Não são fonte — prettier não deve gateá-los (evita falhar o `validate` local em arquivo gerado).
**/.cache/
.governance/specs/0024-context-architecture/work-graph-model/_org-simulation-v2/_viewer/db.json
.governance/specs/0024-context-architecture/work-graph-model/_org-simulation-v2/_viewer/public/snapshot.json

# Artefatos gerados pela governance-demo (mock-api local + relatórios Playwright).
# São gitignored nos subprojetos, mas o `prettier --check .` da raiz não deve
# tratar estes outputs mutáveis como fonte versionada.
.governance/specs/0024-context-architecture/work-graph-model/governance-demo/mock-api/.data/
.governance/specs/0024-context-architecture/work-graph-model/governance-demo/test/reports/
.governance/specs/0024-context-architecture/work-graph-model/governance-demo/test/test-results/
