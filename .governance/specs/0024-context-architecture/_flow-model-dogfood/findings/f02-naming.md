---
node: finding
id: F-G25-02
raised-by: intent-brief
investigated-by:
  - research/2026-06-24-intent-brief-work-initiation-artifact.md
resolves-into: DEC-0024-G25 §D2
status: Convergido
---

# F-G25-02 — Naming: `spec` (kind) e `spec.md` (doc) carregam viés de spec-kit?

**Observação (convergida):** doc → `intent-brief`; pilar `spec → delivery`; umbrella **mantém `kind`**.
Largar o jargão "MECE" na fala → _"7 tipos de trabalho / pilares de valor"_.

**Alternativas que competiam (resolvidas):** `feature`/`capability` (escala fixa SAFe — descartadas);
`type`/`pillar` para a umbrella (descartadas pelo **scan do código**: `type` quebra a convenção `XxxKind`
e colide com o `type` de decisão do `decide/`).

**Evidência:** benchmark de naming + scan de 1021 ocorrências de `kind` (ver `investigated-by`).
