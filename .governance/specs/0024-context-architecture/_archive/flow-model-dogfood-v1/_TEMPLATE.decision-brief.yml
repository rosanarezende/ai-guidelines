# decision-brief — VIEW DERIVADA de decisions/*.md. NÃO editar à mão (regenerada).
# Rastreável tipo `state`: status + progresso k/N por decisão. É a "Resumo de status", derivada.
decisions:
  - id: dec-NNN
    title: <…>
    status: draft # draft | partial | resolved
    questions:
      resolved: [q-NNN]
      open: [q-NNN]
    progress: "k/N"
    owner: null # vem da decision quando resolved
    date: null # no corpo da decision quando resolved
