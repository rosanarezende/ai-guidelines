<!-- ai-guidelines-template: plan-boilerplate v=1 -->

# Plan — Spec 0024 Context Architecture

> Spec: [`./spec.md`](./spec.md)
> Decision Brief: [`./decision-brief.md`](./decision-brief.md)
> Tasks: [`./tasks.md`](./tasks.md)
> Status: Draft

> **Vive durante a execução.** Diferente da `spec.md` (imutável após In Review),
> este arquivo é atualizado conforme o entendimento técnico evolui.

> ## 📖 Glossário operacional (SSOT — Checkpoint 2.1, 2026-05-31)
>
> Vocabulário canônico da 0024, para eliminar a ambiguidade entre _unidade de implementação_ e _Pull Request real_ (drift que a 0023 já havia diagnosticado — cf. `review.md` R6: _"drift 'PR6' não existe"_). Os três eixos são independentes:
>
> - **PR / `#N`** — exclusivamente um **Pull Request real do GitHub** (ex.: `#32`, `#33`).
> - **Checkpoint N** — uma **unidade de implementação** da spec (ex.: Checkpoint 3). Substitui o antigo e ambíguo "PR-N". A enumeração `Checkpoint 1…12` é a trilha de absorção.
> - **Gate** — o **ritual de validação** que fecha um checkpoint: revisões exigidas pela `.governance/review-policy.yml` (quando aplicável) + **Human Gate** (owner). O Human Gate de um checkpoint decide o **próximo movimento** (tipicamente: avançar / abrir o próximo PR _stacked_) — **não** é merge em `main`. O merge em `main` é **evento único ao fim da spec** (modo `unit`, ADR 0024), gateado por `review.md` R8.
>
> **Proveniência** (comentários nos PRs): `ref: #<PR> @ <sha>` · `checkpoint: <N>` · `role: <implementation|technical_audit|architectural_review|human_gate>`. **Template canônico do comentário de checkpoint:** [`.core/process/checkpoint-comment-template.md`](../../../.core/process/checkpoint-comment-template.md).

> ## 🔁 Nota de fase — ABSORÇÃO + MODELAGEM REABERTA / TRILHA DE CONVERGÊNCIA (2026-06-03, `[DEC-0024-G08]`) — _vigente_
>
> Os eixos `G03/G04/G05` foram **reabertos para modelagem** (ver `spec.md § nota de fase` + `decision-brief § [DEC-0024-G08]`). A **trilha de execução** deixou de ser a sequência de absorção 3–12 isolada e passou a ser o **programa de convergência orientado a grafo** — a **SSOT estrutural é `state.yml § topology`** (que já reconcilia: `#32`–`#35` concluídos + a cauda de Continuidade Operacional planejada). Os checkpoints de absorção originais (3–12) foram **dobrados** nos PRs de convergência onde se sobrepõem (mapeamento nos comentários de cada nó da topologia).
>
> **Trilha viva (SSOT = `state.yml § topology`; reconciliada em 2026-07-13):**
> **Concluídos na stack:** `#32` → `#33` → `#34` → `#35` → `co-reconcile` → `co-knowledge` → `bootstrap-compiler` → `toolchain-simplification` → `co-projection` → `co-enforcement` → `co-flow-convergence` (#43, seq 10) → `co-flow-continuation` (#44, seq 11) → `artifact-taxonomy-and-model-review-contract` (#45, seq 12). **Nó ativo:** `internal-architecture-refactor-ddd-bdd` (#46, seq 13), que fecha a arquitetura interna behavior-preserving, a gramática operacional G01 do framework e o graph snapshot derivado. Dentro da continuação semântica, `broad-flow-falsification` e `continuation-review-human-gate` ainda vêm depois do checkpoint ativo; são etapas governadas em `tasks.md`, não novos nós topológicos. **Cauda planejada, sem reordenação:** `dualroot-collapse` (seq 14) → `co-capture` (seq 15) → `co-events` (seq 16) → `housekeeping` (seq 17) → `knowledge-readiness` (seq 18) → `integration-final` (terminal). Ex-nós 37–41 permanecem reescritos na CO; **workitem-registry-persistido + provenance-persistida seguem eliminados** (INV-4 → frontier/projeção derivada).
>
> A tabela "Sequência de Checkpoints" abaixo é **registro histórico** do plano de absorção pré-reabertura; vale como referência do _quê/por quê_ de cada item absorvido, mas a **ordem e o veículo** vivem na topologia. **⚠️ A cauda (ex-nós 37–41) foi REESCRITA como Continuidade Operacional (Opção A, Human Gate #35, 2026-06-07): a tabela 3–12 abaixo é PRÉ-CO — o _quê/por quê_ de cada item segue válido, mas a forma final (nós CO + faxina + eliminados por INV-4) vive na `state.yml § topology`.**
>
> ---
>
> ## 🔁 Nota de fase — ABSORÇÃO OPERACIONAL (2026-05-31, Checkpoint 2)
>
> **Stage 1 (research → decision-brief → gate) encerrou.** As decisões estão `Resolved` (`[DEC-0024-G00]`/`G02`/`G06`); o gate de research está **fechado**; `state.yml` = `implementation`/`closed`. A 0024 está em **absorção operacional**: fazer o sistema refletir decisões já tomadas, removendo divergências decisão↔código **uma a uma**.
>
> O Checkpoint 2 **dobrou a sequência de absorção para dentro deste arquivo** (§ "Sequência de Checkpoints"), aposentando a dependência do plano local efêmero (`~/.claude/plans/`). **A partir daqui, o plano executável canônico da 0024 é este `plan.md`.** O backlog de divergências é o **relatório de auditoria do Codex** (referência decisão↔código).
>
> As seções research-first abaixo foram **colapsadas para sumário histórico** (Stage 1 — encerrado). A pesquisa estrutural ainda aberta vive em [`research/findings.md`](./research/findings.md) e **não bloqueia** a absorção.

---

## 🛰️ Stage 1 — ENCERRADO (sumário histórico)

> **Stage 1 (Research → opções → Gate).** Coletou evidência via análise comparativa de sistemas externos (Hermes, Cursor, opencode, Spec Kitty, Multica) + auditoria interna; consolidou findings convergidos em [`research/findings.md`](./research/findings.md); cravou as decisões no [`decision-brief.md`](./decision-brief.md) (reestruturado **por estado** em 2026-05-31). **Termina no gate humano** — fechado em 2026-05-31.
>
> **Resultado cravado (gate):** `[DEC-0024-G00]` identidade (a unidade primária é a transformação `contexto humano → governança executável`) · `[DEC-0024-G02]` taxonomia `deterministic/mixed/evidence-driven` **removida** (→ bloco + propriedade `exige-julgamento`, marcador `(julgamento)`/`(determinístico)`) · `[DEC-0024-G06]` contrato da cadeia `research → … → implementação`. Todas `Resolved`.
>
> **Pesquisa estrutural ainda aberta** (ex-`G01` estrutura/gramática, ex-`G03` promotion pipeline, ex-`G04` casa única de boilerplate, ex-`G05` projeções, + eixos de pressão A/B/D/E/F): migrou para `research/findings.md` como **findings abertos**. Não é ponto de gate; só retorna ao brief como `[DEC] Pendente` ao convergir + exigir julgamento. **Aceitar `G00` não as resolve** (instrução explícita do gate).

> **Correção de framing (2026-05-31).** O texto pré-absorção dizia "esta spec é research-first; a implementação vira spec separada (0025)". **Isso foi superado:** a implementação é entregue **dentro da 0024** (decisão da owner, 2026-05-29), agora como **absorção operacional** das decisões cravadas — não como pesquisa nem como spec futura.

---

## 🧱 Sequência de Checkpoints — CONGELADA (plano executável canônico)

> **⚠️ Projeção derivada — NÃO é a SSOT estrutural (`[DEC-0024-G07]`).** A **estrutura** da topologia — qual PR, `role`, posição no stack (`sequence`), terminalidade e quais checkpoints cada PR embarca — é **dado em [`state.yml § topology`](./state.yml) (SSOT única)**. Esta tabela é **leitura humana**: as colunas "PR real"/"Status" e a ordenação são **projeção derivada** dessa SSOT (sem auto-sync — em divergência, `state.yml` vence). O valor próprio da tabela é o **Objetivo/Deps** de cada checkpoint (o _quê/por quê_), que **não** vive no `state.yml`.
>
> Disciplina: cada checkpoint **atômico, reversível**, fechado por um **Gate** completo conforme review-policy (`implementation → revisões exigidas → Human Gate`). **1 checkpoint por vez**, parar no Gate quando o commit estiver pronto. Backlog = relatório de auditoria do Codex (códigos `A#/B#/C#/D#/P#`).

| Checkpoint | Objetivo                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Deps             | PR real                | Status                                                                     |
| :--------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------------- | :--------------------- | :------------------------------------------------------------------------- |
| **1**      | `active-specs.yml` lista a 0024 (Codex A3) — publica a spec no índice público via `workflow publish-state`                                                                                                                                                                                                                                                                                                                                                                                                                                            | —                | **#32**                | ✅ feito + gated (`87865ca`)                                               |
| **2**      | reframe `tasks/plan/NEXT/spec` → absorção (Codex A2/P1/P2); `spec.md` via nota datada sem apagar histórico; **dobrar esta sequência no `plan.md`**                                                                                                                                                                                                                                                                                                                                                                                                    | —                | **#32**                | ✅ feito + gated (`8b0eec6`)                                               |
| **2.1**    | correção de vocabulário (PR-N → Checkpoint N; ritual → Gate) + alinhamento à 0023 (separação implementação/revisão/integração)                                                                                                                                                                                                                                                                                                                                                                                                                        | Checkpoint 2     | **#32**                | ✅ feito + gated (`d686d4b`)                                               |
| **2.1a**   | **correção arquitetural pós-dogfooding** — substitui a topologia incorreta do Checkpoint 2.1 (que não era `unit` nem `sequential` e contradizia ADR 0020/0024) pela **topologia canônica da 0023** (`unit`/_stacked_/merge único ao fim; #32 não mergeia cedo)                                                                                                                                                                                                                                                                                        | Checkpoint 2.1   | **#32**                | ✅ feito + gated (`fa2c0ae`)                                               |
| **2.2**    | **correção estrutural pós-dogfooding (precedência sobre o 3)** — `ruleset:check`: **producibilidade** (PRIMÁRIO — todo required context tem produtor _estável_; no `validate`) + **paridade** (SECUNDÁRIO — vivo↔versionado, detect-only no `ruleset-drift.yml`) + **ruleset-as-code** (`.github/rulesets/main-governance.json`) + agregador estável `smoke`. Fecha a classe do `guardrails` órfão (`[DEC-0024-F04]`; Camada 0 já aplicada ao ruleset vivo)                                                                                           | Checkpoint 2.1a  | PR próprio _(stacked)_ | ✅ feito + gated (incorporando Checkpoint 2.2b)                            |
| **2.2b**   | **correção da fronteira mecânica (achado Codex PR #33)** — garante conectividade ao evento-base (`pull_request`/`push`) e tolerância a provedores externos declarativos, prevenindo falsos produtores sem criar um workflow engine (protegendo o limite do ADR 0020).                                                                                                                                                                                                                                                                                 | Checkpoint 2.2   | Mesmo PR do 2.2        | ✅ feito + gated                                                           |
| **2.3**    | **Governance Topology as Data** — Transforma a topologia do PR em dado estruturado no `state.yml` e resolve ambiguidade visual do `governance-pr-check` (Skipped -> Success). Título e template de PR passam a ser tratados arquiteturalmente como projeções verificáveis da topologia.                                                                                                                                                                                                                                                               | Checkpoint 2.2b  | **#33** _(stacked)_    | ✅ implementado; Architectural Review pediu B1/B2 → absorvidos no **2.3a** |
| **2.3a**   | **correção derivada do 2.3 (Architectural Review Gate)** — reconcilia topologia↔realidade (B1: remove nó-fantasma, dobra Checkpoints em #33), registra `[DEC-0024-G07]` (B2), elimina dual-SSOT do `plan.md` (O1 — esta projeção), invariantes de `sequence` no schema (O6), robustez do `governance-pr-check` sem parser (O2), `governance-pr-check` advisory (O5)                                                                                                                                                                                   | Checkpoint 2.3   | Mesmo PR (**#33**)     | ✅ feito + gated (#33)                                                     |
| **2.3b**   | **guard local de lifecycle-coerência** (`github_pr` ⟺ `active`/`concluded`; unicidade) + reavaliação do O5 → promoção do `governance-pr-check` a `required` (apply consolidado: `repo-validation`+`smoke`+`governance-pr-check`; paridade verde)                                                                                                                                                                                                                                                                                                      | Checkpoint 2.3a  | Mesmo PR (**#33**)     | ✅ feito + gated (#33)                                                     |
| **2.4**    | **revisão-como-artefato (MVP dogfood)** — reviews/gates = artefatos versionados (`reviews/c<N>-<role>.yml`, `gates/c<N>.yml`); findings embutidos com ciclo de vida; consolidado **derivado** por `review:check` (no `validate`); gate `approved` exige zero `critical/high` `open`; PR → 1 comentário de status. Comentões de proveniência somem. Dogfood: AR do c2.3 em `reviews/c2.3-arch.yml`                                                                                                                                                     | Checkpoint 2.3b  | Mesmo PR (**#33**)     | ✅ feito + gated (#33)                                                     |
| **2.4a**   | **endurecimento da revisão-como-artefato** (achados AGY/ChatGPT) — separa **finding** (reviewer-owned; `disposition` destrava o gate) de **resolução** (implementer-owned; não destrava) = anti-autoaprovação; `findings_emitted`+ids contíguos = anti-deleção; `location` obrigatória; `fingerprint` sela a claim = anti-rewrite silencioso. Limite honesto: tamper-_evidence_, não prevenção (ADR 0021)                                                                                                                                             | Checkpoint 2.4   | Mesmo PR (**#33**)     | ✅ feito + gated (#33)                                                     |
| **2.4b**   | **fecha bypass da "poda final"** (achado AGY na rev. do 2.4a) — **`review_fingerprint`** (envelope `checkpoint\|role\|count\|ids`) sela o conjunto → deletar a cauda + decrementar count = vermelho (sem acoplar findings); **checkpoint+role no per-finding hash** = anti-transplante. Rejeitados: `findings_emitted` em cada finding, camada git-history, `location@<sha>`                                                                                                                                                                          | Checkpoint 2.4a  | Mesmo PR (**#33**)     | ✅ feito + gated (#33)                                                     |
| **2.4c**   | **2 bugs da auditoria final** — (1) fingerprint via **serialização canônica** (`JSON.stringify`), fim da ambiguidade de `\n` interno em `location`/`description`; (2) resolução aponta finding **totalmente qualificado** (`<role>#<id>`) com match **exato**, fim da colisão cross-role do `endsWith`. Migra schema/parser/templates/README/dogfood/testes                                                                                                                                                                                           | Checkpoint 2.4b  | Mesmo PR (**#33**)     | ✅ feito + gated (#33)                                                     |
| **2.4d**   | **disclosure de IA como projeção de PROCESSO** — renderer puro (`disclosureRender`) que DERIVA de `consolidate()` ∩ checkpoints do PR na topologia: nº de revisões, categorias, findings emitidos/resolvidos, gate humano + estado. **Zero artefato/schema novo** (rejeitados `participants.yml`/ledger — over-modeling; e `Co-Authored-By` como fonte — autoria ≠ participação). Frase "assistida por IA" = editorial do template. Comando `yarn disclosure`; **fora do `validate`** (gerador, não check). Dogfood no #33; Checkpoint 3 nasce usando | Checkpoint 2.4c  | Mesmo PR (**#33**)     | ✅ feito + gated (#33)                                                     |
| **3**      | **GG-0003 Consistency Projection Check** — estritamente mecânico: `state.yml` é fonte de verdade + lista FIXA de marcadores literais contraditórios (`"Stage 1 ativo"`, `"gate aberto"`, brief `Pendente`…). **Sem parsing semântico.** Nasce dogfoodando a revisão-como-artefato (2.4→2.4d) — disclosure derivado, sem texto manual.                                                                                                                                                                                                                 | Checkpoint 2.4d  | PR próprio _(stacked)_ | pendente                                                                   |
| **4A**     | **Workflow Provenance · storage** — `.governance/runtime/provenance.yml` (append-only, runtime-scoped). Campos: `spec`(obrig.)/`actor`/`role`/`at`/`ref`, `model?` opcional. **`role` = string LIVRE**. **Espinha** `implementation`+`human_gate` = sempre DERIVADA (git/active-specs), não persistida, nunca bloqueia. **Opcionais** persistidos (audit/review/security/…).                                                                                                                                                                          | Checkpoint 1/2   | PR próprio _(stacked)_ | pendente _(protótipo em stash — referência)_                               |
| **4B**     | **Workflow Provenance · projeção** no `workflow continue`: impl (derivada) + `Auditorias/revisões registradas:` (lista de tamanho variável) + gate (derivado) + `Revisão independente: PENDENTE/OK`. Mostra **fatos/pendências**, NUNCA prescrição (DEC-0023-B06 lookup-not-coordination). **Provenance = projeção, NÃO compliance/bloqueio/lifecycle.**                                                                                                                                                                                              | Checkpoint 4A    | PR próprio _(stacked)_ | pendente                                                                   |
| **5**      | **AGENTS sync** — `agents:build` (recompila bloco `<AI_GUIDELINES>` de `rules.json`) + `agents:check` (gate no `validate`). Destrava Checkpoint 7.                                                                                                                                                                                                                                                                                                                                                                                                    | —                | PR próprio _(stacked)_ | pendente                                                                   |
| **6**      | **GG-0002 mecanismo** — `banned-concept-check` + registro `banned-by-dec.yml` (sem termo live ainda) + fixture. **Antes** da remoção (instala o guard sem janela de regressão).                                                                                                                                                                                                                                                                                                                                                                       | —                | PR próprio _(stacked)_ | pendente                                                                   |
| **7**      | **CRÍTICO (Codex A1/P0)** — remover taxonomia `evidence-driven/deterministic/mixed` de `GR-0101`→`AGENTS.md`, `governance-foundation §"Tipos de spec"→"Propriedades de bloco"`, `spec-boilerplate` ×2; **registrar o ban no mesmo commit** (sem janela de regressão).                                                                                                                                                                                                                                                                                 | Checkpoint 5 + 6 | PR próprio _(stacked)_ | pendente                                                                   |
| **8**      | corrigir path morto na msg do `gate-decidability-check` (→ `governance-foundation § Guardrails`) (Codex B1)                                                                                                                                                                                                                                                                                                                                                                                                                                           | — (flex)         | PR próprio _(stacked)_ | pendente                                                                   |
| **9**      | desacoplar existência do `decision-brief` de `evidence-driven/mixed` (Codex B3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Checkpoint 7     | PR próprio _(stacked)_ | pendente                                                                   |
| **10**     | tasks boilerplate **único**; aposentar 3 variantes; renomear recipe/partials `tasks-evidence-driven`→genérico (Codex C2/C3)                                                                                                                                                                                                                                                                                                                                                                                                                           | Checkpoint 7     | PR próprio _(stacked)_ | pendente                                                                   |
| **11A**    | drift-guard dos legados `.specify/templates` + `.ai-guidelines/` (estanca a hemorragia) (Codex C1)                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Checkpoint 10    | PR próprio _(stacked)_ | pendente                                                                   |
| **11B**    | trocar fonte ativa → root canônico (⚠️ **micro-decisão da owner:** `.core/templates` como source empacotado vs `.governance/templates` como root consumidor)                                                                                                                                                                                                                                                                                                                                                                                          | Checkpoint 11A   | PR próprio _(stacked)_ | pendente                                                                   |
| **11C**    | remover legados `.specify/templates` + `.ai-guidelines/` após 11B estável                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Checkpoint 11B   | PR próprio _(stacked)_ | pendente                                                                   |
| **12**     | limpar docs arquiteturais de `workflowType` (ARCHITECTURE.md, ADR 0014 nota histórica) (Codex B2/D1)                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —                | PR próprio _(stacked)_ | pendente                                                                   |

**Ordem de valor:** Checkpoint 1 ✅ → 2 ✅ → 2.1 ✅ → 2.1a ✅ → **2.2 + 2.2b** ✅ → **2.3 + 2.3a (topologia-as-data + reconciliação SSOT)** → (3 consistência + 4A/4B proveniência: barreiras novas **antes** da migração) → 5 + 6 → **7 (o crítico, protegido)** → 9/10 → 11A/B/C (`.specify` + `.ai-guidelines`) → 8/12 (flex).

> ⚠️ **Protótipo do Checkpoint 4A em `git stash`** (sessão 2026-05-31): interrompido por **ordem de execução** (não rejeição). Material de **referência** apenas. Quando a sequência chegar ao Checkpoint 4A, inspecionar e decidir o que reaproveitar. **Não** misturar antes da hora nem antecipar Provenance.

---

## 🗺️ Topologia operacional (CANÔNICA — modelo da 0023; corrigida no Checkpoint 2.1a)

> **⚠️ Doutrina (modelo), não a instância.** A topologia **concreta** desta spec — os nós, números de PR, `role`, `sequence`, checkpoints embarcados — é **dado em [`state.yml § topology`](./state.yml) (SSOT, `[DEC-0024-G07]`)**. Esta seção descreve o **modelo** (modo `unit`/_stacked_, papéis, fronteiras) — o _porquê_ da forma; os valores concretos vivem no `state.yml` e o `governance-pr-check` os enforça (advisory).

> **A 0024 aterrissa como UNIDADE** — modo **`unit`** (default da ADR 0024), honrando ADR 0020: _"merge final é ponta a ponta; thinking PR isolado não representa software pronto; a unidade de release é a stack inteira, mergeada como unidade."_ Separa as três fronteiras da 0023 (`[DEC-0023-M01]`): **implementação** (PRs _stacked_ + `tasks.md` execution-only) · **revisão** (Gates por checkpoint + `review.md` na integração) · **integração** (Integration PR terminal + `release-log.md`).
>
> ⚠️ **Esta seção corrige a topologia que o Checkpoint 2.1 havia cravado** (incremental/`sequential`, #32 mergeando cedo, PRs off-`main`). Aquela estrutura **não era `unit` nem `sequential`** e contradizia ADR 0020 + ADR 0024 + o comportamento real da 0023 (`#18…#26` _stacked_ + Integration `#27`, merge único no fim). Ver § "Decisões revisitadas".

- **PR #32 = PR de governança/bootstrap + linha de integração da spec.** Acumula Checkpoint 1 + 2 + 2.1 + 2.1a. **NÃO mergeia antecipadamente** — permanece aberto como a **base da stack** (`feat/spec-0024-context-architecture`) até o encerramento da spec. Como o `#18` da 0023, é a raiz do _stack_, não um PR que aterrissa sozinho.
- **Checkpoint 3 em diante = Pull Requests reais próprios, _stacked_ sobre a linha da spec** (base = branch da 0024 / checkpoint anterior — **não** off-`main` independentes), formando **stack linear** (ADR 0020 §3: "execution PR tem thinking PR como base branch"). Abrem como **Draft** (CORE-09); título `[🛠️] [Spec 0024] Checkpoint N — …`. **Nenhum mergeia em `main` isoladamente.**
- **Gate por checkpoint = fronteira de decisão do PR:** `implementation` (Draft) → revisões exigidas pela `.governance/review-policy.yml` (quando aplicável) → `Human Gate` (owner). Na 0024, usamos Technical Audit + Architectural Review como dogfood intensivo; isso **não** vira obrigação universal para todo repositório. O Human Gate **decide o próximo movimento** (tipicamente: avançar / abrir o próximo PR _stacked_) — **não** aterrissa em `main`. Registrado como proveniência **no próprio PR**.
- **Cada PR carrega o estado:** marca seu item `[x]` em `tasks.md` e atualiza `state.yml.next` para o próximo checkpoint (o estado anda junto com a entrega — sem drift SSOT→projeção).
- **Integration PR (`[🔗] [Integration]`) = boundary de revisão + pré-condição do merge final** (como o `#27` da 0023). Aberto **no fim**, após `review.md` R1–R7 fechados. Homologa a convergência da 0024 ponta-a-ponta. **Não é veículo de aterrissagem** (ADR 0024).
- **Merge em `main` = EVENTO ÚNICO ao fim**, modo `unit`: após `review.md` R8 (autorização de merge da **spec inteira**, owner), o **PR terminal de implementação** é o veículo (carrega o diff acumulado por construção); `#32` + checkpoints intermediários + Integration PR são encerrados via **`landed-via reconciliation`** (não rejeitados — seus commits entram via o veículo). **Um SHA canônico**; rollback = `git revert <SHA>` (1 comando desfaz a spec inteira). O commit de encerramento (spec→`Done`, `state.yml`→`done`, `NEXT.md` deletado, `release-log.md` T0) entra na branch antes do merge único.

---

## 🏗️ Princípios de absorção (lente para cada checkpoint)

> O "como" de cada checkpoint deriva das decisões cravadas. Estes princípios — extraídos do uso real da stack na 0024 — governam as escolhas de design ao longo da sequência:

- **Toda aresta SSOT→projeção precisa de um gate de sync, ou diverge em silêncio.** Drift silencioso SSOT→projeção é o padrão recorrente nº 1 (apareceu 4×: `state.yml`↔`active-specs`, `rules.json`↔`AGENTS.md`, `state.yml`↔`tasks/plan/NEXT/brief`, código↔`ARCHITECTURE.md`). Checkpoint 3 (consistência) e Checkpoint 5 (AGENTS sync) são instâncias.
- **Enforcement previne a classe, não só o sintoma.** O guard entra **antes/junto** da correção, sem janela de regressão (Checkpoint 6 antes de Checkpoint 7).
- **Validação mecânica vs semântica por guardrail.** Todo guardrail declara seu subconjunto 🤖 (check falha) e 👁 (julgamento humano), como GG-0001. Checks de absorção são **estritamente mecânicos** (lista fixa de marcadores literais; sem parsing semântico — Checkpoint 3).
- **Projeção, não governança.** Proveniência (Checkpoint 4A/4B) é **projeção** (fatos/pendências num olhar), nunca compliance/bloqueio/lifecycle. A espinha (impl+gate) é **derivada**, não persistida.
- **Anti-taxonomia em todos os níveis.** Ao remover uma taxonomia fechada, não recriá-la um nível acima; preferir **propriedade livre + papéis reconhecidos (hints de projeção)** a enum fechado (`role` livre na proveniência; `exige-julgamento` por bloco em G02). _Aplicado também a este vocabulário: "Checkpoint" reusa linguagem natural, não introduz sigla nova._
- **Clean-clone/smoke gateia distribuição.** `test:smoke` deve gatear checkpoints que afetam o que o consumidor recebe (notadamente Checkpoint 11A/B/C).

---

## ✅ Critérios de Aceite Detalhados (DoD operacional)

### Por checkpoint

- [ ] Atômico, mergeável, reversível — uma divergência decisão↔código por checkpoint (ou um bloco coeso).
- [ ] Fecha o **Gate** completo conforme a `.governance/review-policy.yml` antes de avançar para o próximo checkpoint. No perfil `solo`, Human Gate é obrigatório e reviews independentes são opcionais em PRs de implementação; no PR final de integração/perfis colaborativos, a política pode exigir review independente além do Human Gate. (O merge em `main` é **único, ao fim** — não por checkpoint.) Correção local pequena não espera; mudança estrutural sim.
- [ ] Contrato operacional sincronizado (`script-contracts:check`) e `yarn validate` verde (format:check + build:rules + suíte + living-docs + state-yml + gate-decidability + checks de governança).
- [ ] Checkpoint que instala/altera guardrail carrega **check + fixture** no mesmo commit; checkpoint que afeta distribuição roda `test:smoke`.
- [ ] Do Checkpoint 3 em diante: **PR real próprio _stacked_** sobre a linha da spec (Draft → Ready → Human Gate → **avançar**; **sem merge isolado em `main`**), com proveniência no PR (`ref: #N @ sha` · `checkpoint: N` · `role`).
- [ ] Sem reabrir decisão `Resolved` (G00/G02/G06) nem re-modelar arquitetura congelada.

### Globais (toda a spec)

- [ ] Pipeline `yarn validate` verde, com hooks/contrato operacional instalados para commits locais.
- [ ] Não-objetivos cravados em `spec.md` continuam respeitados (auditoria final).
- [ ] **Fronteira modelo ≠ migração:** absorção entrega enforcement + artefato de referência; migração ampla do ecossistema (Grupo B) permanece nas candidatas re-escopadas. _(Revisada 2026-06-15 por `[DEC-0024-G09]` **só** para o runtime `/cli`: sua eliminação entra na 0024 via CO-3.5; o resto do Grupo B segue fora.)_
- [ ] `review.md` (R1–R7) + Integration PR + `release-log.md` instanciados no encerramento; spec→`Done` só após o último checkpoint.

---

## 🧪 Estratégia de Testes

> A fase de absorção **produz código** (checks, guardrails, projeções). Cada checkpoint estrutural carrega seus próprios testes:

- **Checks/guardrails** (Checkpoint 3 GG-0003, Checkpoint 6 GG-0002, Checkpoint 8): fixture determinística (caso que falha + caso que passa); integração no chain `yarn validate`.
- **Proveniência** (Checkpoint 4A storage, 4B projeção): testes de serialização append-only + derivação da espinha (impl/gate) sem persistência; projeção mostra fatos/pendências, nunca prescrição.
- **AGENTS sync** (Checkpoint 5): `agents:check` falha quando `<AI_GUIDELINES>` diverge de `rules.json`.
- **Script contracts** (absorvido no PR #37 / `checkpoint-script-contracts`): contrato SSOT para scripts, hooks, workflows, templates e docs; `script-contracts:sync` projeta superfícies; `script-contracts:check` entra no `validate`.
- **Runtime bootstrap readiness** (absorvido no PR #37 / `checkpoint-runtime-bootstrap-readiness`): P0 de bootstrap marcado no `knowledge-backfill.yml`; `handoff` determinístico vira o ponto de entrada situado; `AGENTS.md` vira stub seguro sem carregar o kernel monolítico.
- **Knowledge readiness** (nó final pré-integração): `knowledge:health` gera dossiê determinístico + prompt para revisão assistida por IA; o Human Gate decide duplicatas/supersessões. `co-knowledge:integrity` só vira required se cumprir critérios de determinismo, escopo estreito, mensagem acionável, evidência de uso, fixtures e aprovação humana.
- **Migração `.specify` + `.ai-guidelines`** (Checkpoint 11A/B/C): `test:smoke`/clean-clone valida o que o consumidor recebe.
- **Regressão da remoção** (Checkpoint 7): o ban (`banned-concept-check`, Checkpoint 6) falha se a taxonomia reaparecer.

---

## 🛠️ Arquivos modificados (esperado)

### Stage 1 (encerrado — registro)

- `spec.md` (instanciação + nota de fase) · `decision-brief.md` (DECs → `Resolved`, por estado) · `research/*.md` + `research/findings.md` · `state.yml` (→ `implementation`/`closed`) · `roadmap/backlog.md` · `.gitignore` (`temp/`).

### Absorção (por checkpoint; cf. tabela § "Sequência de Checkpoints")

- **#32 (Checkpoint 1/2/2.1/2.1a):** `spec.md`, `plan.md`, `tasks.md`, `NEXT.md`, `state.yml` + `.core/process/checkpoint-comment-template.md` (governança/docs — linha de integração da spec; não mergeia cedo).
- **Checkpoint 3+ (PRs próprios _stacked_ sobre a linha da spec):** runtime/checks (`cli/governance/*`), boilerplates (`.core/templates/*`, recipes/partials), doutrina (`.core/process/governance-foundation.md`), `AGENTS.md`, `.governance/runtime/provenance.yml`, docs arquiteturais — escopados por checkpoint.

---

## ⚠️ Riscos técnicos (concretos)

| Risco                                                                         | Mitigação                                                                                                                                                     |
| :---------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Voltar a re-modelar em vez de remover divergências (risco dominante da fase). | Critério cravado: _"já foi validado empiricamente na 0024?" → absorver/enforçar/executar._ Não re-pesquisar o `Resolved`.                                     |
| Encadear vários checkpoints estruturais sem Gate.                             | 1 checkpoint atômico por vez; parar no Gate quando o commit estiver pronto; as revisões exigidas são derivadas da review-policy, não hardcoded.               |
| Reusar "PR" para unidade de implementação (drift de vocabulário).             | Glossário canônico no topo; `PR/#N` = GitHub, `Checkpoint N` = unidade, `Gate` = ritual. A 0023 já tinha nomeado (review R6).                                 |
| Remover taxonomia abrindo janela de regressão.                                | Checkpoint 6 (guard `banned-concept-check`) **antes/junto** da remoção (Checkpoint 7); ban ativado no mesmo commit.                                           |
| Proveniência virar compliance/bloqueio/lifecycle.                             | Projeção, não governança; espinha derivada; `role` livre; nunca bloqueia (DEC-0023-B06 lookup-not-coordination).                                              |
| F4b semântico depender de memória humana solta.                               | Nó final `knowledge-readiness`: `knowledge:health` produz dossiê + prompt de revisão assistida; humano decide equivalência.                                   |
| Backfill mínimo virar desculpa para não navegar o histórico.                  | Backfill amplo P0/P1 condicionado como nó final antes de `integration-final`; plano em `research/2026-06-08-knowledge-health-promotion-and-backfill-plan.md`. |
| Scripts/package.json/docs/workflows divergirem e criarem gates ilusórios.     | `checkpoint-script-contracts` no PR #37: SSOT `.core/governance/script-contracts.yml`, sync/check determinístico e validação no `validate`.                   |
| Migração `.specify`/`.ai-guidelines` quebra o que o consumidor recebe.        | Drift-guard (Checkpoint 11A) estanca antes do cutover; `test:smoke`/clean-clone gateia 11B/C.                                                                 |
| Plano voltar a depender de estado local efêmero.                              | Sequência embutida neste `plan.md` (SSOT no repo); `~/.claude/plans/` aposentado como dependência.                                                            |

---

## 📐 Decisões revisitadas

- **2026-05-31 — framing research-first → absorção operacional (Checkpoint 2).** O plano dizia "research-first; implementação vira 0025". Revertido: implementação entregue **dentro da 0024**, agora como absorção das decisões cravadas. Stage 1 encerrado no gate; placeholder Stage 2 substituído pela sequência de checkpoints acima.
- **2026-05-31 — dependência de plano local → SSOT no repo (Checkpoint 2).** A sequência executável vivia em `~/.claude/plans/` (efêmero). Dobrada neste `plan.md`.
- **2026-05-31 — vocabulário "PR-N" → "Checkpoint N" (Checkpoint 2.1).** "PR-N" conflitava com Pull Request real do GitHub (a 0023 já diagnosticara: `review.md` R6). Cravado o glossário (PR/#N · Checkpoint · Gate). **Esta parte permanece válida.** ~~A topologia que o 2.1 também cravou (#32 encerra/mergeia cedo; Checkpoint 3+ off-`main` independentes; modo `sequential`)~~ foi **SUPERADA pelo Checkpoint 2.1a** (ver abaixo) — estava incorreta.
- **2026-06-01 — O5 reavaliado: o guard de _well-formedness_ da topologia é LOCAL, não débito de API (Checkpoint 2.3a → 2.3b).** O `[DEC-0024-G07]` condicionou a promoção do `governance-pr-check` a `required` a um "guard inverso topologia↔realidade", tratado como **bloco único deferido**. Reavaliação: esse bloco conflava duas coisas separáveis — **(i) well-formedness da SSOT** (a topologia é interna e lifecycle-coerente) e **(ii) paridade com a realidade GitHub** (o `github_pr` aponta a um PR real/aberto/com a branch). A parte **(i) é puramente local e determinística** — invariantes no `workflowStateSerializer`, dentro do `state-yml:check`/`repo-validation` (**já required**) — **absorvida no Checkpoint 2.3b** (`github_pr` ⟺ `active`/`concluded`; unicidade de `github_pr`; + O6). A parte **(ii) exige nova camada (API)** e segue evolução futura **não-bloqueante**. **Consequência:** a justificativa que mantinha o `governance-pr-check` _advisory_ **por well-formedness desapareceu** — uma SSOT malformada agora **falha um check já required** antes de qualquer enforcement de projeção. Logo, **promoção a `required` recomendada e EXECUTADA** (apply consolidado 2026-06-01, autorizado pela owner: `required` = `repo-validation` + `smoke` + `governance-pr-check`). O `smoke` do 2.2 — que **nunca fora aplicado** ao ruleset vivo (drift descoberto aqui) — foi aterrissado no mesmo movimento; paridade vivo↔versionado **verde**. A paridade-API (ii) **não** era pré-condição e segue como hardening futuro.
- **2026-06-11 — reordenação CO-4 ↔ CO-3: `co-projection` antes de `co-enforcement` (decisão da owner, durante o PR #39).** Evidência da própria spec: 9 handoffs manuais em 10 dias até 2026-06-07 e, depois, retomada por mega-prompts da owner a cada sessão (o humano seguia como reconciliador — o problema-raiz do reframe CO); trocas reais de agente (antigravity/gemini no Technical Audit, claude-code no Architectural Review, fluxo Codex/ChatGPT nos checkpoints); o renderer de handoff chegou a apontar para handoff stale de 3 nós atrás (PIT-0010, 5 ocorrências → graduado a **GG-0004**; PIT-0011 "contrato executável invisível"). **Sem dependência técnica** de CO-4 sobre EnforcementBinding: o projetor situado deriva de `state.yml`/git/reviews/knowledge já materializados (`src/cli/handoff.ts` é a prova viva — existe e funciona sem CO-3); o `discover()/consolidate()` do reviewCheck já é exportado/reusado. CO-3 segue depois (seq 9), inalterado no conteúdo — continua herdando o resíduo do monólito (AR-2). `sequence` 8↔9 trocados em `state.yml § topology`; contiguidade preservada. Investigação (veredito C) demonstrou que a topologia cravada no Checkpoint 2.1 **não era `unit` nem `sequential`** e **contradizia ADR 0020** (_"a unidade de release é a stack inteira, mergeada como unidade"; "PRs são stacked"_), **ADR 0024** (default `unit`; `sequential` é override de escolha humana explícita) e o **comportamento real da 0023** (`#18…#26` _stacked_ + Integration `#27`, merge único no fim; nem o bootstrap `#18` aterrissou cedo). Corrigido: **#32 não mergeia cedo (é a linha de integração/base da stack); Checkpoint 3+ = PRs _stacked_ sobre a linha da spec (não off-`main`); modo `unit`; Integration PR = homologação antes do merge; merge em `main` = evento único ao fim (R8).** **Aprendizado de dogfooding:** "absorção incremental" (autoria) ≠ "aterrissagem incremental em `main`" — confundir os dois foi a raiz do drift. **Reinterpretação do Human Gate do Checkpoint 2.1:** a decisão da owner ("manter o #32 como trilho de integração até o encerramento via Integration PR") estava, na verdade, **alinhada à arquitetura correta (0023)** — eram os artefatos cravados no 2.1 que divergiam. Logo, **deixa de ser lida como "exceção deliberada"**: era **conformidade** com a 0023, não desvio.
- **2026-06-15 — `/cli` será ELIMINADA (não mais "wrapper/compatibilidade"); `/dist` = única superfície executável (decisão da owner; `[DEC-0024-G09]`).** Supersede o desenho do nó `bootstrap-compiler` (#38, narrado em § 28: "`/cli` passa a wrapper/compatibilidade") e a fronteira "modelo ≠ migração" **para este runtime especificamente**. Evidência operacional: o cutover de roteamento (#35) migrou o `CommandRegistry` para `/src`, mas NÃO a execução — `/cli` ainda contém runtime, wizard (`cli/cli/args.mjs`), provisionamento (`cli/app`/`features`/`fs`/`formatters`), filesystem e ~30 entrypoints de check; `BootstrapCommand` depende de `LegacyExecuteFn`/`loadLegacyExecute` (`cli/app/engine.mjs`); lista duplicada de comandos (`SUPPORTED_MODES` × registry); `package.json` `bin`/`files`/`imports` publicam/executam fonte de `/cli`. Uma "compatibilidade" que é a implementação real é uma 2ª arquitetura executável. **Veículo:** sub-checkpoint próprio **CO-3.5 — colapso integral do runtime CLI** no MESMO nó (`co-enforcement`)/PR (#42)/Gate (modo unit). **Topologia externa (`state.yml`) inalterada** — muda só a sequência INTERNA de sub-checkpoints; NÃO há spec/nó novo. O CO-3.4 permanece ESTRITO (advisory do recibo nas 2 superfícies). Não confundir com a migração ampla do Grupo B (que segue fora da spec) — G09 revisa a fronteira **só** para o runtime `/cli`.
- **2026-06-16 — novo nó planejado `co-flow-convergence` antes de `co-capture` (`[DEC-0024-G10]`).** O CO-3.5 fechou tecnicamente o colapso do runtime CLI, mas o dogfood da Spec 0024 revelou uma classe recorrente de conflito: **dois caminhos tentando explicar o mesmo estado** (`handoff` × `work`, `work` × `decide`, readiness inferida por findings antigos, recibo reescrito pelo fluxo que deveria validá-lo, checks/comandos com critérios diferentes, rotas antigas e novas convivendo até CO-3.5). Antes de automatizar captura/eventos (`co-capture`/`co-events`), a owner decidiu inserir um PR próprio para revisar e corrigir a modelagem ponta a ponta do fluxo governado. Escopo do nó: inventário real → modelo canônico → confronto modelo × código → correção integral dos gaps → dogfood ponta a ponta → falsificação → Human Gate. A máquina de estados a modelar cobre nova sessão → handoff → work → implementação → review → resolution → disposition → avanço de sub-checkpoint → Ready → Human Gate → transição de nó → integração → merge, com fato de entrada, autoridade, comando, efeito permitido, artefato alterado, validação, próximo estado e ações proibidas por transição. Consequência topológica então vigente: `co-enforcement` seq 9 → `co-flow-convergence` seq 10 → `co-capture` seq 11 → `co-events` seq 12 → `housekeeping` seq 13 → `dualroot-collapse` seq 14 → `knowledge-readiness` seq 15 → `integration-final` terminal. **Reordenada depois por `[DEC-0024-G18]`**: `dualroot-collapse` passa a vir imediatamente após `co-flow-convergence`.
- **2026-06-16 — CO-10.2 deixa de ser apenas matriz e passa a entregar convergência coesa inicial (`[DEC-0024-G12]`).** O dogfood de CO-10.2 mostrou que uma etapa puramente inventarial repetiria o problema que o nó foi criado para eliminar: `work`, `decide`, cockpit, wizard e readiness poderiam continuar explicando o mesmo estado por caminhos diferentes até CO-10.3. A owner decidiu que o confronto modelo × código deve mapear as heurísticas locais e, quando a divergência já estiver comprovada na superfície humana/next-action, substituir a regra local pela derivação comum no mesmo ciclo. Consequência: `npm run flow` vira a superfície humana local canônica; o wizard Clack, o cockpit textual, `work`, `decide` e readiness devem consumir a mesma derivação quando aplicável; regras locais substituídas devem ser removidas, não preservadas como camada paralela. CO-10.3 permanece responsável pelos CFG-\* remanescentes e pela correção integral das transições que ainda exigirem matriz/decisão/falsificação adicional.
- **2026-06-17 — CO-10.5 passa a ser UX, linguagem humana e convergência do wizard (`[DEC-0024-G13]`).** O dogfood de CO-10.4 mostrou que a convergência técnica do fluxo ainda expunha termos internos e camadas legadas ao humano: `cockpit`, `briefing`, `review governado`, `comando mutante`, `wizard`, `Entrypoints com adapter runtime` e um caminho avançado que parecia outro produto. A owner decidiu inserir sub-checkpoint próprio antes da falsificação final para transformar `npm run flow` em experiência guiada com Clack, menu por intenção, explicações de bloqueio e provisioning contextual. A antiga etapa `CO-10.5 — falsificação + Human Gate` passou inicialmente a `CO-10.6`; a decisão posterior `[DEC-0024-G14]` subdividiu esse fechamento em fluxo de time, falsificação ampla e revisão independente/Gate. Ready/Human Gate/merge seguem fora deste ajuste.
- **2026-06-18 — CO-10.6 passa a cobrir fluxo de time, múltiplas specs e criação de spec nova (`[DEC-0024-G14]`).** O uso visual do site e o dogfood do `flow` mostraram um gap de produto antes do Gate: o sistema já orienta a spec ativa, mas ainda não explica bem o que acontece quando há várias specs abertas, quando uma pessoa quer continuar uma spec específica ou quando precisa iniciar uma spec nova. Em contexto de time, isso é risco de mis-binding: a pessoa pode trabalhar no PR/branch/spec errados ou depender de memória fora do runtime. A owner decidiu inserir esse escopo antes da falsificação final. Consequência: a antiga `CO-10.6 — falsificação + Human Gate` é dividida em `CO-10.7 — falsificação ampla do fluxo` e `CO-10.8 — revisão independente + Human Gate`. A revisão final deve incluir Technical Audit, Architectural Review e Security Review por outra LLM, com prompts desenhados para buscar falsificações não óbvias e reduzir viés antes de qualquer Ready/Human Gate.
- **2026-06-19 — CO-10.7 passa a ser CLI pública autoexplicável e wizard orientado ao contexto (`[DEC-0024-G15]`).** O redesign do site tornou o produto mais compreensível, mas revelou outra inversão: o site ainda podia explicar uma experiência ideal enquanto a pessoa usuária teria de decorar comandos diretos (`init`, `adopt`, `update`, `work`, `specs`, `peer-review`). A owner decidiu que a porta de entrada real do produto é `npx ai-guidelines`: a CLI deve detectar contexto (pasta vazia, repo existente, repo governado, perfil solo/time, specs ativas, branch/tree/PR/CI/reviews/readiness) e orientar o próximo passo em linguagem humana. O site deve reproduzir essa experiência como documentação viva, não substituir lacunas do wizard. Consequência: falsificação ampla passa a `CO-10.8` e revisão independente + Human Gate passa a `CO-10.9`; comandos diretos continuam como atalhos, não como experiência principal.
- **2026-06-19 — CO-10.8 passa a ser arquitetura interna, organização DDD e BDD visual para mantenedores (`[DEC-0024-G16]`).** O dogfood do site React mostrou que organização de arquivos também é experiência humana: quando a estrutura fica difusa, a owner perde capacidade de revisar, entender fluxos e encontrar gaps. A mesma classe apareceu no runtime: `src/cli` concentra arquivos grandes (`flowWizard.ts`, `workflow.ts`, `workBrief.ts`, `handoff.ts`, `reviewBrief.ts`) e testes extensos em posições pouco navegáveis, apesar da intenção DDD original de separar delivery, app, domain e infrastructure. A owner decidiu inserir uma etapa estrutural antes da falsificação ampla para produzir inventário robusto da árvore atual, árvore-alvo proposta, plano de refactor behavior-preserving e preparação BDD visual para humanos/mantenedores. Consequência: falsificação ampla passa a `CO-10.9` e revisão independente + Human Gate passa a `CO-10.10`. Possíveis gaps/features descobertos durante a pesquisa devem ser registrados em artefato exclusivo e só promovidos a novo checkpoint mediante decisão posterior; não entram automaticamente no refactor estrutural.
- **2026-06-19 — CO-10.7 reaberto antes da retomada de CO-10.8 (`[DEC-0024-G17]`).** O seed de CO-10.8 mostrou organização interna útil, mas a owner identificou que CO-10.7 havia sido fechado antes do critério pretendido: provar a experiência pública de `npx ai-guidelines` em consumidores reais/simulados e refletir essa experiência no site/documentação. Consequência: CO-10.7 volta a ser o sub-checkpoint ativo; CO-10.8 fica pausado, com o commit `2d478b2` preservado como seed antecipado; o próximo trabalho volta a ser o harness de consumidor/CLI pública, incluindo cenários `consumer-empty`, `consumer-existing-package`, `consumer-existing-formatter-conflict`, `consumer-governed-solo`, `consumer-governed-team`, `consumer-governed-multiple-specs` e `consumer-peer-review`.

- **2026-06-20 — `dualroot-collapse` antecipado para antes de `co-capture`/`co-events` (`[DEC-0024-G18]`).** O dogfood de CO-10.7 mostrou que a CLI pública e o site ainda precisavam explicar a ponte `.ai-guidelines`/`.specify`, enquanto `co-capture` e `co-events` automatizariam o fluxo sobre essa estrutura temporária se permanecessem na ordem antiga. Consequência então planejada: o PR #43 não absorveria o colapso e seguiria CO-10.8, CO-10.9 e CO-10.10; o próximo PR planejado depois de `co-flow-convergence` passaria a ser `dualroot-collapse`, dedicado a migrar config/templates/specs/runtime bootstrap/fixtures/docs para a casa canônica antes de CO-5/CO-6. **Revisada por `[DEC-0024-G19]` e materializada por `[DEC-0024-G20]`:** o PR #43 para no recorte CO-10.1..CO-10.7; CO-10.8..CO-10.10 seguem para o nó `co-flow-continuation` antes de `dualroot-collapse`.
- **2026-06-20 — recortar PR #43 em CO-10.1..CO-10.7 e pedir revisão externa antes do Gate (`[DEC-0024-G19]`).** Após concluir CO-10.7, a owner reavaliou o tamanho do PR #43 e decidiu não misturar a entrega de CLI pública/site/harness com a reorganização estrutural de CO-10.8 e a falsificação ampla subsequente. Consequência: CO-10.8, CO-10.9 e CO-10.10 deixam de ser pendências executáveis dentro do PR #43 e viram continuação de próximo PR; o próximo passo do PR #43 é revisão externa independente do recorte entregue, para avaliar riscos e coerência antes da decisão humana. Esta decisão não executa Ready, Human Gate, merge, `advance-subcheckpoint`, `finish-subcheckpoint`, `mark-readiness` nem abertura automática de PR.
- **2026-06-21 — revisão externa exige reconciliar body, `next` e topologia antes de Ready (`[DEC-0024-G20]`).** A revisão externa independente confirmou que o recorte CO-10.1..CO-10.7 é coeso, mas apontou que `state.yml § next` ainda narrava retomada por G17, o body do PR ainda subdeclarava a entrega e CO-10.8..CO-10.10 não estavam ancorados na topologia. Consequência: registrar o parecer em `research/`, atualizar o body do PR #43, reconciliar `next` e inserir `co-flow-continuation` seq 11 antes de `dualroot-collapse`, empurrando `dualroot-collapse` para seq 12, `co-capture` para seq 13 e `co-events` para seq 14. Esta decisão não implementa a continuação nem executa Ready/Human Gate/merge.
- **2026-06-22 — checkpoints semânticos + sem débito arquitetural silencioso (`[DEC-0024-G21]`).** O dogfood do PR #44 mostrou que a nomenclatura `CO-10.8.*` e o uso difuso de `research/` estavam escondendo decisões diferentes sob um mesmo marcador. A owner decidiu que o PR #44 fecha decisão/inventário/dogfood da continuação, não o refactor DDD/BDD amplo, e registrou **GG-0005 — Sem débito arquitetural silencioso**: decisão arquitetural estruturante precisa virar contrato implementado no mesmo PR, PR/checkpoint governado imediatamente seguinte com aceitação/enforcement explícitos, ou rejeição por DEC. Consequência: o próximo PR deve implementar `artifact-taxonomy-and-model-review-contract` de forma robusta (`kind`/metadado, `research-index:check`, promoção de artefatos maduros, projeções não-autoridade e review pré-codificação/model-review como tipo real ou rejeitado por DEC). O que toca `.specify` fisicamente continua em `dualroot-collapse`, porque a árvore e as referências mostram que essa migração é inseparável do colapso dual-root.
- **2026-06-22 — vocabulário de modelagem `Frente` no lugar de `Fase` (`[DEC-0024-G22]`).** A investigação empírica de colisão (mapa V4 + `research/2026-06-22-checkpoint-artifact-taxonomy-map-v4-nomenclature-review.md`) mostrou que `Fase` colide forte com usos legados do repo, inclusive os headers deste plano e do `tasks.md`. A owner adotou **Spec › Frente › Checkpoint › Etapa › Tarefa**: `Frente` é o termo humano para agrupamento maior que checkpoint, **leitura derivada de `state.yml § topology` — não SSOT nem novo campo**; PR continua contêiner (ADR 0025); níveis intermediários são opcionais; gaps entram via as 5 disposições de `GG-0005`. A mesma decisão reconciliou a tensão do #45 (`sequence: 12` ⇒ é nó topológico ativo; comentário de `state.yml` corrigido). A V4 (`assets/spec-0024-map-v4.html`) é a projeção visual, **não** autoridade. Não implementa taxonomia/model-review. _Os headers "Fase de X" deste plano permanecem como estágios de lifecycle (histórico), distintos de "Frente"._
- **2026-06-23 — extensão de G08 para grafo derivado + entrega incremental + camada de consulta (`[DEC-0024-G23]`).** A auditoria decidido-vs-aberto (`research/2026-06-23-governance-model-question-audit.md`) do compilado de grafo/entrega-incremental/camada-de-consulta mostrou que as três dimensões novas (grafo de governança operacional, prova de valor incremental, snapshot/banco derivado) cabem na 0024 estendendo `[DEC-0024-G08]`, sem nova spec/frente/repo. A owner decidiu manter tudo como **projeção estritamente derivada** (repo = SSOT; sem 2ª SSOT; banco deferido ao spike `knowledge-graph-store-spike`) e rotear os critérios às etapas existentes: forma do grafo + contrato do **graph snapshot derivado** em `internal-architecture-refactor-ddd-bdd`; `fixtures/journeys` + **falsificação de prova de valor** em `broad-flow-falsification`; `kind` compatível com nós do grafo em `artifact-taxonomy-and-model-review-contract` (#45). A sequência **CONGELADA** permanece inalterada; entrega incremental/prova de valor só vira etapa própria se um follow-up concluir que precisa. Não implementa grafo/banco/simulador nem executa Ready/Human Gate/merge/advance.

---

## 📎 Anexo — Conteúdo candidato pré-research

_(Não-aplicável — esta spec nasceu pós-Spec 0023 sem rascunho herdado a reconciliar.)_
