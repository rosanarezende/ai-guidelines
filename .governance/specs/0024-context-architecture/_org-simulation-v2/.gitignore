# Caches/derivados regeneráveis (read-models internos + a view) — NÃO versionar.
# A FONTE é o `.governance/` source; o `context.json` (projeção PUBLICADA) é VERSIONADO (é contrato — freshness via lint no pre-commit).
**/.governance/.cache/
acme-governance/.cache/
**/.governance/*.db
_writecheck/
