# registry/exploration.yml — acme-design-system · índice das explorations deste repo (a GOVERNANÇA do trabalho).
entries:
  - id: exploration-001
    title: "o DS tem form validado? (investigação)"
    status: done # FECHOU (era active) — DISPARA por derivação: q1 resolved → contrato form-component known → deliveries destravam
    # ===== A AMARRA (a comunicação intent ↔ exploration) =====
    answers: acme-governance/intent-0001#q1 # back-ref TIPADO: esta exploration responde à q1 da intent
    # (anota-se 1 lado: a exploration declara `answers`; a intent DERIVA "q1 answered-by exploration-001")
    workspace: works/exploration_form-validation/
    # ===== FECHO (simples: só a resposta, sem POC) =====
    fate: throwaway # só análise, sem código durável (destinos: throwaway | promoted | parked)
    closed-by: works/exploration_form-validation/exploration-answer.md # o verdict (CONTEÚDO) mora aqui; o registry só REFERENCIA
    closed-at: 2026-06-27
    date: 2026-06-27
