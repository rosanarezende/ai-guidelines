# Artefato de REVISÃO (Spec 0024 Checkpoint 2.4 — revisão-como-artefato).
# Copie para `c<checkpoint>-<role>.yml` (ex.: c3-audit.yml, c3-arch.yml).
# 1 arquivo por (checkpoint, role); re-reviews EDITAM este arquivo (git = histórico).
# Findings vivem aqui (embutidos). O estado consolidado é DERIVADO por `review:check`.
checkpoint: "3"
role: technical_audit # technical_audit | architectural_review
actor: codex-cli
model: GPT-5 Codex # opcional
ref: "#33 @ <sha>" # opcional
date: "2026-06-02" # opcional (string)
decision: changes_requested # approved | changes_requested | blocked
summary: "uma linha"
findings:
  - id: F1
    severity: high # critical | high | medium | low
    status: open # open | resolved | accepted | dismissed
    description: "o achado, em uma linha"
  # - id: F2
  #   severity: low
  #   status: resolved
  #   description: "..."
