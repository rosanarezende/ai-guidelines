# Copy this file to .env before running docker compose.
#
# This file configures only the local Postgres container used by the
# shared-portal spike. It is not an application secret store.

GOVERNANCE_PORTAL_POSTGRES_PORT=55432
POSTGRES_DB=governance_portal
POSTGRES_USER=governance
POSTGRES_PASSWORD=change-me-local
