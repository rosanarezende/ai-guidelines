# model-v2-history-2026-07-05

Este diretorio preserva o `model.yml` anterior a limpeza normativa de
2026-07-05.

O arquivo arquivado misturava:

- modelo conceitual vigente;
- historico de provocacoes P9-P13;
- reconciliacoes de auditoria;
- exemplos extensos;
- status de implementacao da `governance-demo`.

Ele foi preservado para auditoria historica. A autoridade corrente voltou para
`../../model.yml`, agora em formato normativo limpo (`meta.version: 3`).

Regra: se este arquivo contradisser o `model.yml` ativo, o `model.yml` ativo
vence.
