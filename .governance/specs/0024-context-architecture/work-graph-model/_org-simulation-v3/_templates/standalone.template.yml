# TEMPLATE standalone — reativo/avulso: SEM intent, SEM abordagem (as camadas colapsam).
# Nada some: o outcome vai pro bucket OPERACIONAL do dashboard.
id: <slug>
kind: fix # fix | incident-response (só por ALERTA verificável) | dep-bump | <preset>
repo: <repo>
origin: "<de onde veio: suporte · alerta · rotina>"
review: interno # derivado como sempre (repo de outro time → externo)
placar: operational-bucket
# incident-response acrescenta: severity · mttr · postmortem · follow-ups (viram peças/intake)
