# trust-policy.yml — política mínima da sim v3.
# Barra: todo item aqui precisa ser resolvido por tools/checks/validate.ts; campo sem executor é cerimônia.

access-requests:
  - id: req-billing-read-own-context
    actor: lead-billing
    action: read-context
    repo: acme-mfe-billing
    decision: allow
    via: host-local-query
    reason: "lead do time dono consulta o contexto publicado do próprio repo"

authority-revocations:
  - authority: lead-support
    revoked-at: 2027-02-01
    reason: "rotação simulada de papel; envelopes posteriores não podem reutilizar esta autoridade"

secret-quarantine: []

oracle-independence:
  corpus: tools/checks/test-adversarial.ts
  attack-by: codex
  expected-by: claude
  approved-by: owner
