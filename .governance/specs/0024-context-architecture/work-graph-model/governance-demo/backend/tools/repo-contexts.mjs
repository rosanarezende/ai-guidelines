// repo-contexts.mjs — shim de compatibilidade; fonte ativa em ../src/adapters/repo-first.
export {
  loadManifest,
  loadPublishedContexts,
  deriveRepoContext,
  deriveAllRepoContexts,
  publishRepoContexts,
  inspectRepoCode,
  validateRepoContexts,
} from "../src/adapters/repo-first/repo-contexts.ts";
