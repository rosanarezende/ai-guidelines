<!-- ai-guidelines-template: tasks-evidence-driven-boilerplate v=4 -->

# Tasks — Spec 0024 Context Architecture

> Spec: [`./spec.md`](./spec.md)
> Plan: [`./plan.md`](./plan.md)
> Decision Brief: [`./decision-brief.md`](./decision-brief.md)
> Status: In Progress — **Absorção operacional** (Stage 1 encerrado; gate de research fechado)

> **Progress file vivo.** Atualizar a cada degrau concluído. Quando uma decisão mudar, refletir em `plan.md § Decisões revisitadas` e ajustar tasks impactadas.

> ## 📖 Vocabulário (cf. `plan.md § Glossário operacional`)
>
> **PR / `#N`** = Pull Request real do GitHub · **Checkpoint N** = unidade de implementação da spec · **Gate** = ritual de validação conforme review-policy + Human Gate. _"PR-N" foi aposentado (Checkpoint 2.1) — conflitava com Pull Request real; a 0023 já diagnosticara (review R6)._

> ## 🔁 Nota de fase — ABSORÇÃO OPERACIONAL (2026-05-31, Checkpoint 2)
>
> **Stage 1 (research → decision-brief → gate) encerrou** com as decisões `Resolved` (`[DEC-0024-G00]`/`G02`/`G06`) e o **gate fechado**. A execução agora é a **sequência de Checkpoints de absorção** (SSOT em [`plan.md` § "Sequência de Checkpoints"](./plan.md)), não mais o scaffold research-first.
>
> A **Fase 0** abaixo é **registro histórico** do bootstrap + Stage 1. O marcador de template (`tasks-evidence-driven-boilerplate`) e o rótulo `evidence-driven` são **legados** — a taxonomia foi removida por `[DEC-0024-G02]`; sua eliminação do recipe/partials é **execução derivada** (Checkpoint 10), não ocorre aqui. A execução viva está na **Fase de Absorção**.

---

## Fase 0 — Setup + Stage 1 (Research → Decision-Brief → Gate) — ✅ ENCERRADA

> **Resultado (gate fechado, 2026-05-31):** decisões cravadas `[DEC-0024-G00]` (identidade = transformação `contexto humano → governança executável`), `[DEC-0024-G02]` (taxonomia de tipos removida → bloco + propriedade `exige-julgamento`), `[DEC-0024-G06]` (contrato da cadeia) — todas `Resolved`. Pesquisa estrutural ainda aberta migrou para `research/findings.md` (não bloqueia).

### Sub-bloco [0.Setup] — Bootstrap e instanciação ✅

- [x] **0.1** Bootstrap: `roadmap/backlog.md` + `governance-foundation.md § "Tipos de spec"` lidos na sessão de planejamento (2026-05-28).
- [x] **0.2** Tipo de spec confirmado na instanciação: `evidence-driven` (research-first). _Nota: rótulo hoje legado — taxonomia removida por `[DEC-0024-G02]`._
- [x] **0.3** Slug semântico: instanciado como `handoff-as-first-class`; **renomeado para `context-architecture`** (2026-05-30, autorização da owner; nº 0024 imutável, ADR 0017).
- [x] **0.4** Branch `feat/spec-0024-context-architecture` criada a partir de `main`.
- [x] **0.5** `spec.md` instanciado; campo `Decision Brief` aponta para `./decision-brief.md`.
- [x] **0.6** Validação humana inicial de problema/escopo — **coberta pelo gate fechado** (decisões `Resolved` em `decision-brief.md`).
- [x] **0.7** `plan.md` instanciado (Stage 1/Stage 2 placeholder → **dobrado na sequência de Checkpoints**, Checkpoint 2).
- [x] **0.8** `tasks.md` (este arquivo) instanciado.
- [x] **0.9** `decision-brief.md` instanciado; depois **reestruturado por estado** (2026-05-31), DECs `Resolved`.
- [x] **0.10** `roadmap/backlog.md` atualizado (handoff → `Em execução`).
- [x] **0.11** `NEXT.md` instanciado.
- [x] **0.12** Pull Request **#32** criado (PR de governança/bootstrap da 0024).
- [x] **0.[COMMIT]** Commits da sessão de instanciação registrados (git).

### Sub-bloco [0.Research → 0.Gate] — Stage 1: research → opções → gate ✅ (superado pela reforma do brief)

> O scaffold granular research-first (`0.R.*` / `0.B.*` / `0.G.*`) foi **superado pela reforma por estado** do `decision-brief.md` (2026-05-31): perguntas abertas migraram para `research/findings.md` (findings abertos) e as decisões convergidas foram cravadas `Resolved`. Histórico verbatim: **git** + research datadas.

- [x] **Stage 1 — research produzida:** inventário arquitetural + comparativos (Hermes, Cursor, opencode, Spec Kitty, Multica) + findings convergidos (`F-001..F-014`) em `research/findings.md`.
- [x] **Stage 1 — decisões cravadas:** `[DEC-0024-G00]` / `[DEC-0024-G02]` / `[DEC-0024-G06]` `Resolved`; brief reestruturado por estado.
- [x] **Gate humano de research fechado** (2026-05-31, @rosanarezende): `decision-brief.md § Gate — assinaturas`.
- [x] **`state.yml`** atualizado para `stage: implementation` / `gate.status: closed`.
- [x] **plan.md / tasks.md "v2"** = reframe de absorção + sequência de Checkpoints (Checkpoint 2) + correção de vocabulário (Checkpoint 2.1).

---

## Fase de Absorção — execução da sequência de Checkpoints (Stage 2, dentro da 0024)

> **Execução viva.** Remover divergências decisão↔código uma a uma. Detalhe canônico de cada checkpoint (objetivo, deps, ordem de valor, PR real, princípios) em [`plan.md` § "Sequência de Checkpoints"](./plan.md). Cada checkpoint estrutural: atômico/reversível + **Gate** completo (Technical Audit → Architectural Review → Human) + `yarn validate` verde. **1 checkpoint por vez; parar no Gate.**
>
> **Topologia (cf. `plan.md § Topologia operacional` — CANÔNICA / 0023, corrigida no Checkpoint 2.1a):** **#32 = PR de governança/bootstrap + linha de integração da spec** (acumula Checkpoint 1 + 2 + 2.1 + 2.1a) — **NÃO mergeia cedo**; é a **base da stack**. **Checkpoint 3 em diante = PR real próprio _stacked_** sobre a linha da spec (não off-`main`), Draft → Gate → **avançar**. **Merge em `main` = evento único ao fim** (modo `unit`; veículo = PR terminal; demais via `landed-via reconciliation`), após `review.md` R8.

> ## 🧭 Reconciliação de topologia (2026-06-09 · AR-1) — SSOT = `state.yml § topology`
>
> A **sequência viva e autoritativa** de nós/checkpoints é `state.yml § topology` (não esta lista). A cauda original (antigos Checkpoints 3–12: GG-0003, Workflow Provenance, AGENTS sync, GG-0002, remoção de taxonomia, decouple-brief, tasks boilerplate, dual-root, doc cleanup) foi **REESCRITA em 2026-06-07** (Human Gate #35, Opção A) no laço de **Continuidade Operacional (CO)** + **faxina (housekeeping)** + `dualroot-collapse` + `knowledge-readiness` + `integration-final`. O histórico `[x]`/`[/]` abaixo (Checkpoints 1…2.4c) é **registro real** das landings em **#32**/**#33**; em particular, **#33 (`ruleset-producibility`) e seus checkpoints 2.2–2.4c CONCLUÍRAM** (`state.yml § topology.concluded`) — os marcadores `[/]` abaixo são, portanto, históricos. Desde então: **#34** (graph-core), **#35** (cli-cutover), **#36** (co-reconcile), **#37** (co-knowledge) concluídos; **#38** (bootstrap-compiler) ativo.

- [x] **Checkpoint 1** — `active-specs.yml` lista a 0024 (Codex A3). ✅ feito + gated (`87865ca`); Gate completo no **#32**.
- [x] **Checkpoint 2** — reframe `spec/plan/tasks/NEXT` → absorção + dobrar a sequência no `plan.md` (Codex A2/P1/P2). ✅ feito + gated (`8b0eec6`).
- [x] **Checkpoint 2.1** — correção de vocabulário (PR-N → Checkpoint N; ritual → Gate) + alinhamento à 0023. ✅ feito + gated (`d686d4b`).
- [x] **Checkpoint 2.1a** — **correção arquitetural pós-dogfooding**: substitui a topologia incorreta do 2.1 pela canônica da 0023 (`unit`/_stacked_/merge único ao fim; #32 não mergeia cedo). ✅ feito + gated (`fa2c0ae`); o Gate de encerramento do #32 cobriu Checkpoint 2 + 2.1 + 2.1a (reconciliation do FAIL ambiente-dependente do Codex + architectural_review + human_gate).
- [x] **Checkpoint 2.2** — **correção estrutural pós-dogfooding (precede o 3)**: `ruleset:check` — **producibilidade** (PRIMÁRIO, no `validate`) + **paridade** (SECUNDÁRIO, detect-only no `ruleset-drift.yml`) + **ruleset-as-code** (`.github/rulesets/main-governance.json`) + agregador estável `smoke`. **PR próprio _stacked_** (`feat/spec-0024-ruleset-producibility`). ✅ feito + gated (incorporando Checkpoint 2.2b). _(dep: Checkpoint 2.1a)_
- [x] **Checkpoint 2.2b** — **correção da fronteira mecânica (achado Codex no PR #33)**: extração de gatilhos-base (`on: [pull_request, push]`) + suporte declarativo a produtores externos, prevenindo órfãos de evento absoluto sem criar um workflow engine. ✅ feito + gated
- [x] **Checkpoint 2.3** — **Governance Topology as Data**: topologia como dado (SSOT) no `state.yml` + `governance-pr-check` valida título/template como projeções; UX do CI (Skipped→Success/Exempt). Em **#33**. ✅ implementado; o Architectural Review Gate pediu B1/B2 → **absorvidos no 2.3a**. _(dep: Checkpoint 2.2b)_
- [/] **Checkpoint 2.3a** — **correção derivada do 2.3 (Architectural Review)**: reconcilia topologia↔realidade (B1 — remove nó-fantasma, dobra Checkpoints em #33), registra `[DEC-0024-G07]` (B2), elimina dual-SSOT do `plan.md` (O1), invariantes de `sequence` no schema (O6 — única/contígua/`execution`-only), robustez do `governance-pr-check` sem parser (O2). Mesmo PR (**#33**). Implementado; gate pendente cobre 2.3a+2.3b. _(dep: Checkpoint 2.3)_
- [/] **Checkpoint 2.3b** — **guard local de lifecycle-coerência da topologia** (sustenta a promoção do `governance-pr-check` a `required`): `github_pr` ⟺ `active`/`concluded` + unicidade de `github_pr` no `workflowStateSerializer` (regressão explícita do nó-fantasma); **reavaliação do O5** → well-formedness é guard local (não débito de API), promoção a `required` **recomendada**; `NEXT` separado (só a paridade-API resta como hardening futuro). Mesmo PR (**#33**). **EM EXECUÇÃO**; `yarn validate` verde. _(dep: Checkpoint 2.3a)_
- [/] **Checkpoint 2.4** — **revisão-como-artefato (MVP dogfood)**: reviews/gates viram artefatos versionados (`reviews/c<N>-<role>.yml`, `gates/c<N>.yml`) com findings embutidos (ciclo `open/resolved/accepted/dismissed`); consolidado **derivado** por `review:check` (no `validate`); gate `approved` exige zero `critical/high` `open`. PR → 1 comentário de status. Comentões de proveniência (`implementation/audit/review/gate`) **somem** → viram artefatos. Dogfood: a Architectural Review do c2.3 já vive em `reviews/c2.3-arch.yml`. Mesmo PR (**#33**). **EM EXECUÇÃO**; `yarn validate` verde. _(dep: Checkpoint 2.3b)_
- [/] **Checkpoint 2.4a** — **endurecimento da revisão-como-artefato** (achados AGY/ChatGPT): separa **finding** (reviewer-owned, `disposition` destrava o gate) de **resolução** (implementer-owned, NÃO destrava) → anti-autoaprovação estrutural; `findings_emitted` + ids contíguos → anti-deleção; `location` obrigatória (`<path>#L` ou `global`); `fingerprint` sela a claim (severity/location/description) → anti-rewrite silencioso. Limite honesto cravado: tamper-**evidence**, não prevenção (ADR 0021). Dogfood migrado: `c2.3-arch.yml` + `c2.3-resolutions.yml`. Mesmo PR (**#33**). **EM EXECUÇÃO**; `yarn validate` verde. _(dep: Checkpoint 2.4)_
- [/] **Checkpoint 2.4b** — **fecha o bypass da "poda final"** (achado AGY na rev. do 2.4a): **`review_fingerprint`** (envelope `checkpoint|role|findings_emitted|ids`) sela o CONJUNTO → deletar a cauda + decrementar count → vermelho (sem acoplar findings entre si); **checkpoint+role no per-finding fingerprint** → fecha transplante de bloco entre arquivos. Rejeitados: acoplar `findings_emitted` em cada finding (AGY), camada git-history (ineficaz p/ poda no mesmo PR), `location@<sha>` (usabilidade, não bypass). Mesmo PR (**#33**). **EM EXECUÇÃO**; `yarn validate` verde. _(dep: Checkpoint 2.4a)_
- [/] **Checkpoint 2.4c** — **2 bugs de implementação da auditoria final**: (1) fingerprint deixa de ser concatenação textual (`join("\n")`, ambíguo a `\n` interno) e passa a **serialização canônica** (`JSON.stringify([...])`) no per-finding e no envelope; (2) resolução passa a apontar finding **totalmente qualificado** (`<role>#<id>`) com matching **exato** (fim do `endsWith` que colidia cross-role). Migra schema/parser/templates/README/dogfood/testes (+ regressões: quebra-de-linha e `architectural_review#F1` ≠ `technical_audit#F1`). Mesmo PR (**#33**). **EM EXECUÇÃO**; `yarn validate` verde. _(dep: Checkpoint 2.4b)_

  > ### ⤵️ Cauda original (Checkpoints 3–12) — SUPERSEDED em 2026-06-07
  >
  > Os antigos Checkpoints 3–12 **deixaram de ser a fila viva**; foram reescritos no laço CO + faxina. **Mapa para a topologia vigente (`state.yml § topology` é o SSOT — não duplicar a fila planejada aqui):**
  >
  > - **3 · GG-0003 (consistency)** → landou como **#36 `co-reconcile`** (`checkpoint-co-reconcile`; "semente: ex-checkpoint-gg-0003").
  > - **4A/4B · Workflow Provenance** → reenquadrado como **projeção derivada** (provenance re-derivada, não persistida — INV-4); cai em **CO-5 `co-capture`** / **CO-4 `co-projection`**.
  > - **5 · AGENTS sync** → **`housekeeping` › `checkpoint-agents-sync`**.
  > - **6 · GG-0002** → **`housekeeping` › `checkpoint-gg-0002`**.
  > - **7 · remoção de taxonomia (CRÍTICO/P0)** → **`housekeeping` › `checkpoint-taxonomy-removal`** (inclui ex-10, tasks boilerplate).
  > - **8 · path morto do `gate-decidability-check`** → resíduo menor; follow-up de `housekeeping`/doc.
  > - **9 · desacoplar `decision-brief`** → **`housekeeping` › `checkpoint-decouple-brief`**.
  > - **10 · tasks boilerplate genérico** → absorvido em **`checkpoint-taxonomy-removal`**.
  > - **11A/B/C · colapso `.specify` + `.ai-guidelines` / dual-root** → **`dualroot-collapse` › `checkpoint-dualroot-collapse`**.
  > - **12 · limpeza de docs (`workflowType`)** → **`housekeeping` › `checkpoint-doc-cleanup`**.
  >
  > **Forward (autoritativo em `state.yml § topology`):** concluídos na stack até `co-enforcement` (CO-3, PR #42, Human Gate approved) → ativo `co-flow-convergence` (seq 10; **inserido em 2026-06-16 por `[DEC-0024-G10]` antes de `co-capture`**) → planejado `co-flow-continuation` (seq 11; **materializado por `[DEC-0024-G20]` para CO-10.8..CO-10.10 em próximo PR**) → `dualroot-collapse` (seq 12; **antecipado por `[DEC-0024-G18]` para remover a ponte `.ai-guidelines`/`.specify` antes de automatizar captura/eventos**) → `co-capture` (CO-5) → `co-events` (CO-6) → `housekeeping` → `knowledge-readiness` → `integration-final`. Esta lista é **ponteiro**, não 2ª fonte: em divergência, `state.yml` vence.

- [x] **Checkpoint npm-toolchain** (nó `toolchain-simplification`, seq 7, **PR #39**) — ✅ **CONCLUÍDO + gated** (Human Gate approved 2026-06-11, `gates/c-npm-toolchain.yml`; PR #39 Ready, HEAD `d806a835`, CI 11/11; modo unit, sem merge isolado). Entregue: npm puro (`package-lock.json` + `npm ci`; um runner em script-contracts/hooks/CI/release/templates/docs; validate também no Windows; multi-PM preservado nos consumidores com Corepack condicional); FU-1 `pr-body:update` + FU-2 `pr-ready:check`; Template v3 + perfis de PR (`PrProfileContract` SSOT); `review:seal`; PIT-0010 → guardrail GG-0004; PIT-0011 capturado; retomada derivada saneada; `co-projection` reordenado para seq 8. _(próximo: `co-projection` / CO-4)_
- [x] **Checkpoint co-projection** (nó `co-projection`, seq 8 / CO-4, **PR #41**) — ✅ **CONCLUÍDO + gated** (Human Gate approved 2026-06-12, `gates/c-co-projection.yml`; PR #41 Ready, HEAD `46a7ffe7`, CI 11/11; `pr-ready:check` satisfeito; modo unit, sem merge isolado). Entregue: handoff como ato de carga situado (`deriveHandoff`/`deriveNextAction` puros; selo + recibo efêmero; `handoff:check` advisory-first; contrato global na cápsula); briefing governado de reviews + `review:seal`/`review:publish` + verification de dois escopos; catálogo de reviews separado de obrigatoriedade (só required+não-current bloqueia; PIT-0012); `security_review` universal/optional (required em integration por regra local); entrypoint `guidelines` descobrível (--help/FLUXO SITUADO/Intents). 9 rodadas de dogfood em `research/`. _(próximo: `co-enforcement` / CO-3)_ — escopo original autorizado pelo Human Gate do #39 (`gates/c-npm-toolchain.yml`, 2026-06-11; reordenado para antes de CO-3 — `plan.md § Decisões revisitadas`) (`gates/c-npm-toolchain.yml`, 2026-06-11; reordenado para antes de CO-3 — `plan.md § Decisões revisitadas`): handoff situado DERIVADO + contrato de carga (ADR 0022/0021). Escopo: estender o comando `handoff` (modelo puro `deriveHandoff`; reviews/resolutions/gates via `discover()`/`consolidate()`; insights do cursor; branch/HEAD/ahead-behind; próxima ação única + proibições derivadas); `handoff:check` advisory-first com selo de geração (freshness; stale detectável); projeção regenerável em `.governance/runtime/handoff/` somente se confirmada arquiteturalmente; integração AGENTS/runtime bootstrap (descobribilidade — PIT-0011). Zero LLM no runtime; handoff é projeção, não SSOT. PR próprio _stacked_ sobre o #39. _(dep: `toolchain-simplification` / Human Gate do #39)_
- [x] **Checkpoint co-enforcement** (nó `co-enforcement`, seq 9 / CO-3, **PR #42**) — ✅ **CONCLUÍDO + gated** (Human Gate approved 2026-06-16, `gates/c-co-enforcement.yml`; PR #42 Ready, HEAD `46ff89b`, CI 11/11; `pr-ready:check` satisfeito; modo unit, sem merge isolado). Entregue: EnforcementBinding (declaração→enforcement por nó; PIT-0008 estrutural) + `knowledge:compile`; substrato legacy migrado/removido; dogfood advisory-first do enforcement/recibo; CO-3.5 com colapso integral do runtime CLI (`/cli` inexistente, `bin`/scripts em `dist`, `providers` removido, tarball/consumidores validados). Zero LLM no runtime. PR próprio _stacked_ sobre o #41. _(dep: `co-projection` / Human Gate do #41; próximo nó ativo: `co-flow-convergence`, não `co-capture`)_
  - **Sub-checkpoints internos (CO-3, PR #42; Gate único ao fim, modo unit):** fronteiras por dependência (modelo+binding → compilador+manifesto → migração legacy → enforcement+recibo → colapso do runtime CLI); cada um `validate` verde, sem merge isolado.
    - [x] **CO-3.1 — Constraint + EnforcementBinding** (modelo + fonte estruturada): fatia vertical mínima `rule|guardrail → Constraint normalizada → EnforcementBinding (4 campos) → resolução de superfície (`npm-script`/`registry-command`) → resolução de mecanismo → manifesto determinístico em memória`. Fonte `.core/constraints/constraints.yml` (+ overlay opcional `.governance/constraints.yml`); `GovernedRef{space:"surface"}` + aresta `constrains`; `constraints:check` integrado ao `validate`. Dados reais: GG-0001 + 1 rule CORE. **Concluído.**
    - [x] **CO-3.2 — knowledge:compile + manifesto/paridade**: entrypoint humano orquestrando o compilador TS; manifesto runtime persistido + paridade derivada (existência/classe/sync); `build:rules` permanece alias compatível.
    - [x] **CO-3.3 — migração e remoção do substrato legacy**: port TS de `token-budget` + reconexão `check-budget`/features editoriais; deleção do monólito sob `grep monolith = 0` + teste de paridade. **Implementado 2026-06-14:** `token-budget` → `src/app/services/TokenBudget.ts` (stub injetável; paridade golden em `TokenBudget.test.ts`); `getOptInRuleRelativePath` → `src/app/services/OptInRulePaths.ts`; `budget-report.mjs` + features editoriais (`bdd`/`tdd`/`quality-gates`) reconectados via ponte `dist/` (padrão `pointers.mjs`); fixtures `rules-builder` → `src/app/services/__fixtures__/`; `cli/governance/monolith/**` + `index.mjs` + alias `#governance/monolith/*` removidos; guarda `NoMonolithResidue.test.ts` fixa `grep monolith = 0`. `validate` local verde (node:test 183/183, jest 1653/1653). Avanço CO-3.3 → CO-3.4 aguarda `decide advance-subcheckpoint` (humano) + CI verde — não exercido aqui.
    - [x] **CO-3.4 — dogfood do enforcement e recibo**: caminho não-lançante do recibo em `workflow publish-state` (+ `review:publish`), advisory-first; dispatcher amplo fica no CO-6. **Implementado/refinado 2026-06-15** (recuperado de WIP Antigravity em `ffc0e7c`; refinado em `acb6da0`+`7fdf242`): fonte única (`specIdFromLabel`/`describeReceiptStaleReason`/`formatReceiptAdvisory`) + `emitReceiptAdvisory` centralizado (advisory LOCAL, sem rede, ignora a fonte remota `pull-request`); 3 falsificações viradas teste (reescrita silenciosa do recibo no `publish-state`; advisory morto no `review:publish`; falso-positivo na fonte remota); 5 estados provados (unit + integração); dogfood em `research/2026-06-15-checkpoint-co-enforcement-co-3.4-dogfood.md`. Avanço CO-3.4 → CO-3.5 exercido posteriormente pela owner; CO-3.5 é o sub-checkpoint terminal ativo deste PR.
    - [x] **CO-3.5 — colapso integral do runtime CLI** `readiness: ready-for-transition` (`[DEC-0024-G09]`, 2026-06-15): runtime legado removido fisicamente; `/cli` inexistente; `package.json#bin → dist/cli/main.js`; scripts públicos executam `dist`; tarball SEM `package/cli`; `CommandRegistry` como única fonte de comandos/help/interação; `LegacyExecuteFn`/`loadLegacyExecute` inexistentes; `providers` removido sem alias e absorvido por `update --providers`; `init`/`adopt`/`update`/`check-budget` executam em TypeScript; pacote instalado validado em consumidor novo e existente; CI remoto multi-OS verde; guard arquitetural do cutover reforçado. **Critério de saída atendido e Gate do nó aprovado:** `/cli` inexistente; `bin` executa `dist`; wizard vive em `/src`; nenhuma ponte legacy; tarball + consumidores verdes. _(Escopo é o runtime `/cli` — NÃO a migração ampla do Grupo B, que segue fora da spec.)_
  - **Dogfood 2026-06-11 (primeira rodada do PR #41):** primeira retomada via handoff derivado expôs `active.yml` com `branch` 2 gerações stale, mascarado por fallback silencioso de resolução + checker stage-only. Corrigido estruturalmente (commit `5881a85`): invariantes branch/identidade no `active-specs:check`, resolução canônica-primeiro + fallback declarado no handoff, projeção regenerada via `publish-state`. Evidência + 8 requisitos derivados para `deriveHandoff`/`handoff:check`: `research/2026-06-11-checkpoint-co-projection-dogfood-active-specs-drift.md`; PIT-0011 visto 2×.

- [x] **Checkpoint co-flow-convergence** (nó `co-flow-convergence`, seq 10) — **ativo após Human Gate do `co-enforcement` e planejado por `[DEC-0024-G10]` (2026-06-16), antes de `co-capture`**: convergência do fluxo ponta a ponta da Spec 0024 para impedir que `co-capture` e `co-events` automatizem um fluxo ainda parcialmente modelado. Escopo: inventário real → modelo canônico → confronto modelo × código → correção integral dos gaps → dogfood ponta a ponta → fluxo em time/múltiplas specs/criação de spec → CLI pública autoexplicável → arquitetura interna/BDD humano para mantenedores → falsificação ampla → revisão independente + Human Gate. Deve produzir uma única máquina de estados para nova sessão → handoff → work → implementação → review → resolution → disposition → avanço de sub-checkpoint → Ready → Human Gate → transição de nó → integração → merge; matriz explícita conceito/SSOT/projeções/consumidores/validações; invariantes work↔decide, readiness terminal, zero findings, findings antigos, registry/help/wizard/scripts e não-retorno do legado pós-CO-3.5; jornadas reais incluindo consumidor novo/existente, branch/CI/receipt stale, múltiplas specs abertas, criação de spec nova, retomada e modo offline/degradado. PR próprio _stacked_ sobre `co-enforcement`; não é spec separada nem housekeeping; não implementa CO-5.
  - [x] **CO-10.1 — inventário real + modelo canônico**: mapear fatos vivos, estados, autoridades, comandos, efeitos permitidos, artefatos, validações, próximos estados e ações proibidas. Inventário inicial registrado em `research/2026-06-16-checkpoint-co-flow-convergence-co-10.1-inventory.md`.
  - [x] **CO-10.2 — confronto modelo × código + convergência coesa inicial** (`[DEC-0024-G12]`): comparar a máquina de estados canônica com `handoff`, `work`, `decide`, reviews, readiness, PR state, checks, registry/help/wizard/scripts e CI **e corrigir, no mesmo ciclo, as divergências já comprovadas na superfície humana/next-action**. **Entradas:** inventário CO-10.1, reviews/resolutions, `state.yml`, `tasks.md`, registry, comandos vivos e dogfood do fluxo real. **Saídas:** matriz modelo × código com cada transição/gap classificado como `equivalente`, `intencional`, `bug` ou `risco`; `flow` como superfície humana local canônica; cockpit/wizard/work/decide/readiness consumindo a mesma derivação quando aplicável; heurísticas locais substituídas removidas; documentação visual do fluxo (`FLOW.html`) como contrato explicável. **Fronteira:** não iniciar CO-10.3, não resolver todos os CFG-\* neste sub-checkpoint e não executar Ready/Human Gate/merge/advance.
  - [x] **CO-10.3 — correção integral dos gaps remanescentes**: corrigir divergências reais ainda classificadas em CO-10.2 sem criar segunda SSOT ou heurísticas paralelas. **Entradas:** matriz CO-10.2 atualizada, protocolos CFG-001..CFG-009, bounded context `GovernedLifecycleFlow`, matriz de ameaças e dogfood de `flow`. **Saída:** runtime/checks/docs convergindo em snapshot/policies comuns para `handoff`, `work`, `decide`, `pr-ready`, `review:check`, registry/help e transições de nó.
  - [x] **CO-10.4 — dogfood ponta a ponta**: exercitar jornadas reais de sessão nova, implementação, review, resolution, disposition, Ready, Human Gate, transição, integração, consumidores e retomada. **Entradas:** correções de CO-10.3 e jornadas TDD do inventário. **Saída:** evidência executada por jornada, incluindo consumidores novo/existente, stale/offline/degraded e bloqueios de autoridade.
  - [x] **CO-10.5 — UX, linguagem humana e convergência do wizard** (`[DEC-0024-G13]`): transformar `npm run flow` em uma experiência humana clara e guiada com Clack, sem nova lógica paralela. **Entradas:** dogfood de CO-10.4, feedback da owner sobre termos internos (`cockpit`, `briefing`, `review governado`, `wizard`, `comando mutante`, `Entrypoints com adapter runtime`) e exemplos avançados do Clack. **Saída:** menu por intenção humana, resumo simples legível, opções bloqueadas explicadas, init/adopt/update contextuais, providers/features por seleção agrupada, validações com tarefas visuais e fluxo avançado compatibilizado sem expor o wizard legado como produto separado. **Fronteira:** não executar Ready, Human Gate, merge ou avanço; não criar regra local de readiness/CI/PR state no wizard.
  - [x] **CO-10.6 — fluxo de time, múltiplas specs e criação de spec nova** (`[DEC-0024-G14]`): modelar e implementar a orientação governada para desenvolvimento em time antes do fechamento do nó. **Entradas:** dogfood de CO-10.5, pergunta da owner sobre várias specs abertas e criação de nova spec, `state.yml`, active specs, branch/PR atual, review policy e superfícies `flow`/`handoff`/`work`/`decide`. **Saída:** `flow` orienta a pessoa a escolher/continuar a spec correta, iniciar uma spec nova pelo caminho governado, entender bloqueios de branch/PR/CI/tree e diferenciar autoridade de contributor/maintainer/team; múltiplas specs não deixam `work` inferir silenciosamente o alvo errado; o fluxo deixa claro quando uma ação exige Rosana/maintainer. **Fronteira:** não abrir CO-5, não executar Ready/Human Gate/merge, não iniciar novo PR automaticamente, não criar lock distribuído falso. **Dogfood:** `research/2026-06-18-checkpoint-co-flow-convergence-co-10.6-team-flow.md` registra a convergência inicial do wizard para specs abertas, continuação explícita e spec nova fail-closed.
  - [x] **CO-10.7 — CLI pública autoexplicável e wizard orientado ao contexto** (`[DEC-0024-G15]`; reaberto por `[DEC-0024-G17]`): garantir que `npx ai-guidelines` seja a porta de entrada pública do produto, sem exigir que a pessoa decore comandos. **Entradas:** dogfood do site como documentação viva, revisão da separação público/contribuidor, `src/cli/copy`, registry, `flowWizard`, provisioning `init/adopt/update`, active specs, review policy, `GovernedFlow` e cenários reais de consumidor. **Saída:** CLI/wizard detecta contexto e orienta a pessoa em linguagem humana: pasta vazia, projeto com arquivos soltos, repo existente sem ai-guidelines, conflitos, repo já governado, perfil solo/contribuições/time, uma spec ativa, múltiplas specs, ausência de spec, branch errada, tree suja, PR Draft/Ready, CI verde/pendente/falha, findings/reviews/resolutions/dispositions, readiness, review de PR de colega e modo offline/degradado. **Subcenários obrigatórios de uso diário:** nova sessão em repo governado; escolher foco com múltiplas specs; retomar após interrupção; validar tree suja; resolver finding; fechar resolution/disposition; declarar readiness; avançar quando permitido; bloquear avanço por readiness/findings/CI/branch; preparar Ready sem executar; preparar Human Gate sem executar; revisar PR de colega; atualizar providers/práticas; diferenciar update comum de mudança de política de colaboração. **Critério corretivo:** falsificar a CLI pública em consumidores simulados/instalados (`consumer-empty`, `consumer-existing-package`, `consumer-existing-formatter-conflict`, `consumer-governed-solo`, `consumer-governed-team`, `consumer-governed-multiple-specs`, `consumer-peer-review`), capturar a experiência de `npx ai-guidelines`, provar opções contextuais, dry-run, aplicação quando permitido, bloqueios e mensagens, e refletir isso no site/documentação. **Kickoff/status:** `research/2026-06-19-checkpoint-co-flow-convergence-co-10.7-kickoff.md` e `research/2026-06-19-checkpoint-co-flow-convergence-co-10.7-status.md`. **Fronteira:** não executar Ready, Human Gate, merge, avanço, abertura de PR/spec ou alteração de topologia; comandos diretos seguem como atalhos, não como experiência principal.
  - [x] **Revisão externa pré-Human Gate do PR #43** (`[DEC-0024-G19]`): revisão independente do recorte entregue até CO-10.7 concluída em `research/2026-06-21-checkpoint-co-flow-convergence-pr43-external-review.md`. **Resultado:** o recorte CO-10.1..CO-10.7 foi considerado coeso e sem bloqueio de código, mas Ready/Human Gate dependem de correções de honestidade: body do PR, `state.yml § next`, ancoragem topológica da continuação, disclosure de reviews inventário-scoped e smoke real diferido. **Correção governada:** `[DEC-0024-G20]` materializa `co-flow-continuation` antes de `dualroot-collapse` e reconcilia os artefatos pré-Ready. **Fronteira:** não executar Ready, Human Gate, merge, avanço, readiness, abertura automática de novo PR ou implementação de CO-10.8.
    > **Continuação movida para próximo PR por `[DEC-0024-G19]` e ancorada como nó `co-flow-continuation` por `[DEC-0024-G20]` (não é pendência executável do PR #43):**
    >
    > - **CO-10.8 — arquitetura interna, organização DDD e BDD visual para mantenedores** (`[DEC-0024-G16]`; seed antecipado `2d478b2` preservado): reorganizar estruturalmente o runtime e os testes para que o próprio framework seja legível e sustentável por humanos, sem alterar comportamento nem conteúdo de produto.
    > - **CO-10.9 — falsificação ampla do fluxo**: transformar invariantes em checks/testes e falsificar jornadas não óbvias depois da reorganização estrutural.
    > - **CO-10.10 — revisão independente + Human Gate**: preparar a rodada final de revisão ampla do nó de continuação, sem confundir com a revisão externa do recorte do PR #43.

---

- [/] **Checkpoint co-flow-continuation** (nó `co-flow-continuation`, seq 11, **PR #44**) — aberto por transição governada pós-Human Gate de `co-flow-convergence`; PR próprio _stacked_ sobre `feat/spec-0024-co-flow-convergence`; não mergeia isoladamente em main. **Recorte revisado por `[DEC-0024-G21]`:** a continuação deixa de usar `CO-10.8.*` como nomenclatura operacional e passa a ser acompanhada por checkpoints semânticos. O PR #44 fecha decisão/inventário/dogfood; implementações estruturais subsequentes precisam nascer como PRs governados, sem débito silencioso.
  - [x] **drift-diagnosis-and-repair — Governance Doctor / drift explicável e reparável**: transformar drift governado em diagnóstico e reparo guiado, sem exigir que o usuário conheça `state.yml`, `active.yml`, `tasks.md` ou topologia por dentro. **Primeira falsificação observada:** `open-next-node` criou a branch do próximo nó antes de reconciliar `active.yml`, fazendo o hook bloquear corretamente o push e forçando recuperação manual. **Escopo entregue:** drift #1 reparável com preview/confirm; drifts #2..#7 detectados/explicados quando aplicável, com reparo por decisão humana quando tocam topologia, PR body ou GitHub; autodetecção pelo preflight/`drift:check`; rota pública `/cli/avancado` com casos avançados. **Artefatos:** `research/2026-06-21-checkpoint-co-flow-continuation-kickoff.md`, `assets/drift-tracker.html`, `assets/spec-0024-map.html`.
  - [x] **lifecycle-model-and-artifact-taxonomy-decision — mapa, inventário e falsificação do modelo**: registrar a decomposição semântica da continuação, a fronteira entre PR/checkpoint/nó, a taxonomia de artefatos e o guardrail **GG-0005 — Sem débito arquitetural silencioso**. **Critério de saída do PR #44:** `[DEC-0024-G21]` registrada; mapa V2 e inventários versionados; revisões de falsificação registradas; `tasks.md`/`plan.md`/`state.yml § next` reconciliados; body do PR #44 atualizado para declarar o recorte real. **Artefatos:** `assets/spec-0024-map-v2.html`, `research/2026-06-22-checkpoint-co-flow-continuation-lifecycle-model-inventory.md`, `research/2026-06-22-checkpoint-co-flow-continuation-artifact-taxonomy-inventory.md`, `research/2026-06-22-checkpoint-co-flow-continuation-*-falsification-review.md`, `research/2026-06-22-checkpoint-co-flow-continuation-repo-tree-inventory.md`.
  - [/] **Checkpoint artifact-taxonomy-and-model-review-contract** — implementação robusta da taxonomia: PR #45 governado da continuação. **Fatia zero obrigatória antes da taxonomia:** fechar os gaps de dogfood registrados em `research/2026-06-22-checkpoint-artifact-taxonomy-gap-closure.md` — visão pretendida via `.governance/visual-prompts`, operação governada para abrir o próximo PR interno e decisão de vocabulário humano do lifecycle (`Spec > Fase > Checkpoint > Etapa > Tarefa`, com níveis opcionais quando aplicável). Depois disso, implementar `kind`/metadado equivalente como fonte única da natureza dos artefatos; classificar artefatos atuais relevantes sem mover arquivos referenciados; unificar as ordens de autoridade hoje divergentes (`research/README.md` × inventário de taxonomia) em uma regra canônica por domínio; classificar handoffs antigos como `kind: handoff-legacy` ou equivalente, sem criar nova pasta de handoff persistido; reparar e verificar `research-index`; promover artefatos maduros para `research-library/<domínio>`; rotular mapas/projeções como não-autoridade; materializar o tipo governado de review pré-codificação/model-review com schema/home/autoridade/lifecycle/check/doc/exemplo, ou rejeitá-lo por DEC. **Não é prova mínima.**
  - [ ] **internal-architecture-refactor-ddd-bdd — reorganização behavior-preserving do runtime/testes**: reorganizar estruturalmente `src/cli` e os testes para que o próprio framework seja legível e sustentável por humanos, sem alterar comportamento nem conteúdo de produto. O seed antecipado `2d478b2` continua como material de entrada, mas o refactor não compete com o PR #44.
  - [ ] **broad-flow-falsification — falsificação ampla do lifecycle governado**: transformar invariantes em checks/testes e falsificar jornadas não óbvias depois da reorganização estrutural e da taxonomia de artefatos.
  - [ ] **continuation-review-human-gate — revisão independente + Human Gate**: preparar a rodada final de revisão ampla do nó de continuação, sem confundir com a revisão externa do recorte do PR #43.

## Fase de Review → vive em `review.md`

> **Modelo de 3 boundaries (`[DEC-0023-M01]`):** o **Gate por checkpoint** (4 papéis, em cada PR) é a revisão da implementação daquele PR. A **prontidão de integração da spec** vive em `review.md` (gates R1–R9), **instanciado no encerramento** — agrega/confirma que o conjunto de checkpoints landou coerente, gateia o Integration PR (R1–R7) e o merge de encerramento (R8). `tasks.md` é execution-only e fecha 100% `[x]` ao fim da execução = sequência de Checkpoints (1…12) concluída, dentro da própria 0024.

---

## Fase de Encerramento → vive em `release-log.md` + Integration PR

> **Integration PR (`[🔗] [Integration]`) nasce no fim** (após o último checkpoint + `review.md` R1–R7 fechados): homologa a 0024 ponta-a-ponta — **boundary de revisão + pré-condição do merge final, não veículo de aterrissagem** (ADR 0024). O commit de encerramento (spec→`Done`, `state.yml`→`done`, `NEXT.md` deletado, `release-log.md` T0) entra na linha da spec **antes** do **merge único** (modo `unit`: veículo = PR terminal de implementação; #32 + intermediários + Integration PR via `landed-via reconciliation`; 1 SHA canônico), após `review.md` R8. A **migração ampla do ecossistema (Grupo B)** permanece nas candidatas re-escopadas, não nesta spec (fronteira modelo ≠ migração).
