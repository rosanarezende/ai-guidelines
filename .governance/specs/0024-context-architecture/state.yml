stage: implementation
gate:
  status: closed
focus:
  - "RECONCILIACAO 2026-06-03 ([DEC-0024-G08], owner): G03 (promotion pipeline) / G04 (casa unica/tri-root) / G05 (projecoes) foram REABERTOS. Os eixos deixados em aberto no Stage 1 mostraram-se necessarios para problemas reais observados no uso (mis-binding da retomada, recuperabilidade, 'a resposta estava num ADR esquecido'). A 0024 VOLTOU A TRATAR MODELAGEM nesses eixos -- isto e DECISAO, nao drift. Direcao assumida: arquitetura orientada a grafo (entidades + relacoes navegaveis KnowledgeRef + projecoes derivadas). NAO ha 0025 independente (o split foi superado no Checkpoint 2; o trabalho mapeia em G03/G04/G05 + candidatas Grupo B). O kernel Knowledge + KnowledgeGraph nucleo + capability Insights JA materializados (PR #34). Motivacao persistida como Insights (PIT-0004..0006)."
  - "0024 em ABSORCAO OPERACIONAL (pesquisa encerrada). Problema = falta de absorcao, nao falta de decisao: decisoes convergidas ainda nao alteraram o comportamento do sistema ('arquitetura converge enquanto codigo diverge'). Backlog = relatorio de auditoria do Codex (divergencias decisao<->codigo)."
  - "Plano executavel CANONICO no repo: plan.md secoes 'Sequencia de Checkpoints' + 'Topologia operacional'. VOCABULARIO (Checkpoint 2.1): PR/#N = Pull Request real do GitHub; Checkpoint N = unidade de implementacao da spec; Gate = ritual de validacao (Technical Audit -> Architectural Review -> Human)."
  - "TOPOLOGIA CANONICA (corrigida no Checkpoint 2.1a; modelo da 0023 / ADR 0020 + 0024 / DEC-0023-M01): #32 = PR de governanca/bootstrap + LINHA DE INTEGRACAO da spec; NAO mergeia cedo, e a BASE DA STACK. Checkpoint 3+ = PR real proprio STACKED sobre a linha da spec (NAO off-main, NAO independente). Modo UNIT: merge em main e evento UNICO ao fim (veiculo = PR terminal de implementacao; #32 + intermediarios + Integration PR via landed-via reconciliation; 1 SHA canonico), apos review.md R8. Integration PR = homologacao antes do merge (NAO veiculo)."
  - "Decisoes cravadas (NAO reabrir): G00/G02/G06; Open abolido/DEC nasce Pendente/Deferred canonico; brief por estado; GG-0001 interno (DOGFOOD-*); WorkflowType removido; proveniencia runtime-scoped (role livre, espinha impl+gate derivada, projecao NAO governanca); GG-0002 antes da remocao."
  - "Gate por checkpoint: implementacao -> revisoes exigidas pela .governance/review-policy.yml (quando aplicavel) -> Human Gate (owner) -> AVANCA (abre o proximo PR stacked). Na 0024 usamos Technical Audit + Architectural Review como dogfood intensivo, mas isso NAO e obrigacao universal para todo repositorio. O Human Gate decide o PROXIMO MOVIMENTO, NAO mergeia em main. 1 checkpoint atomico por vez; parar no Gate quando o commit estiver pronto. Correcao local pequena nao espera; mudanca estrutural sim."
  - "Criterio (RESSALVADO em 2026-06-03 por [DEC-0024-G08]): 'ja foi validado empiricamente na 0024?' -> absorver/enforcar/executar. NAO re-modelar o DECIDIDO (G00/G02/G06/G07 seguem cravados). EXCECAO assumida: G03/G04/G05 foram reabertos para modelagem deliberada (orientada a grafo) -- nesses eixos, modelar e a direcao, nao o risco. O 'risco dominante' original (modelar em vez de absorver) vale para os eixos resolvidos; nos reabertos, a modelagem e o trabalho."
next:
  - "canonical-next: internal-architecture-refactor-ddd-bdd. PR #45 (artifact-taxonomy-and-model-review-contract / seq 12) CONCLUIDO-NA-STACK por Human Gate approved (gates/c-artifact-taxonomy-and-model-review-contract.yml, 2026-07-12); modo unit, SEM merge isolado em main. No ATIVO = internal-architecture-refactor-ddd-bdd (seq 13, checkpoint-internal-architecture-refactor-ddd-bdd; PR #46 Draft stacked sobre #45, aberto via continuation:create-pr conforme DEC-0024-G27). Escopo do recorte: reorganizacao behavior-preserving de src/cli e testes; fechar/dispositionar a gramatica operacional remanescente de G01; definir o contrato do graph snapshot derivado (nodes/edges/source-refs/hashes; deterministico, regeneravel, offline; derived-only, sem 2a SSOT) e explicitar quais projecoes de G05 o snapshot atende (DEC-0024-G23/G28). Insumos obrigatorios antes de fechar gramatica/snapshot: work-graph-model/model.yml, tracker.md, features.md e GUILDA-QRD-PRESERVATION-MATRIX.md (aprendizados estruturais apenas; produto Guilda permanece no repo irmao). Depois: broad-flow-falsification e continuation-review-human-gate. NAO implementar dualroot-collapse (agora seq 14), co-capture, co-events, Ready/Human Gate automatico, merge em main ou movimentacao fisica de .specify neste PR."
topology:
  cursor:
    pr: internal-architecture-refactor-ddd-bdd
    checkpoint: checkpoint-internal-architecture-refactor-ddd-bdd
  prs:
    concluded:
      - id: base-governance
        github_pr: 32
        role: governance
        terminal: false
        sequence: null
        checkpoints:
          - checkpoint-1
          - checkpoint-2
          - checkpoint-2.1
          - checkpoint-2.1a

      - id: ruleset-producibility
        github_pr: 33
        role: execution
        terminal: false
        sequence: 1
        checkpoints:
          - checkpoint-2.2
          - checkpoint-2.2b
          - checkpoint-2.3
          - checkpoint-2.3a
          - checkpoint-2.3b
          - checkpoint-2.4
          - checkpoint-2.4a
          - checkpoint-2.4b
          - checkpoint-2.4c
          - checkpoint-2.4d

      # #34 concluido apos Human Gate graph-core approved (gates/c-graph-core.yml, 2026-06-04).
      # Embarcou: capability Insights + kernel Knowledge + KnowledgeGraph nucleo.
      - id: insights-in-flight
        github_pr: 34
        role: execution
        terminal: false
        sequence: 2
        checkpoints:
          - checkpoint-insights
          - checkpoint-knowledge-kernel
          - checkpoint-graph-core

      # #35 CONCLUIDO apos Human Gate cli-cutover approved (gates/c-cli-cutover.yml, 2026-06-07).
      # Cutover CLI -> Command Registry: dispatch+parse dissolvidos; help DERIVADO (o registry e o
      # SSOT do help); fail-fast quando dist/ ausente. Findings #1/#4 da auditoria transferidos ao
      # no bootstrap-compiler (seq 6). Modo unit: NAO mergeou em main (concluido na stack).
      - id: pr-cli-cutover
        github_pr: 35
        role: execution
        terminal: false
        sequence: 3
        checkpoints:
          - checkpoint-cli-cutover

      # #36 CONCLUIDO apos Human Gate co-reconcile approved (gates/c-co-reconcile.yml,
      # 2026-06-07; F1 accepted apos o fix c24a47b). CO-1: reconcile:check + contrato de
      # autoridade (canonical-next: <id>) — 1o guard advisory da Continuidade Operacional.
      # Modo unit: NAO mergeou em main (concluido na stack).
      - id: co-reconcile
        github_pr: 36
        role: execution
        terminal: false
        sequence: 4
        checkpoints:
          - checkpoint-co-reconcile # reconcile:check + contrato de autoridade (U1); semente: ex-checkpoint-gg-0003 (consistency)

      - id: co-knowledge
        github_pr: 37
        role: execution
        terminal: false
        sequence: 5
        checkpoints:
          - checkpoint-co-knowledge # Knowledge tipado + Falsification/fingerprint + aresta constrains (U2)
          - checkpoint-co-knowledge-backfill # CO-2.1: inventario minimo, FAL-0002, grafo por inventario/RulesCatalog, graph-store research
          - checkpoint-script-contracts # CO-2.2: contrato operacional SSOT para package.json/docs/hooks/workflows/templates + script-contracts:check
          - checkpoint-runtime-bootstrap-readiness # CO-2.3: handoff minimo + AGENTS.md stub + P0 runtime_bootstrap_p0 + bootstrap PATH dos hooks
          - checkpoint-runtime-ledger-partition # CO-2.4: specs active/history + insights por status + falsifications/ledger
          - checkpoint-review-policy-events # CO-2.5: review-policy + eventos append-only de re-auditoria/revalidacao

      # #38 CONCLUIDO apos Human Gate bootstrap-registry approved
      # (gates/c-bootstrap-registry.yml, 2026-06-09). Resolveu a auditoria do #35
      # (#1 roteamento, #3 fallback, #4 parsers) e fechou o cutover exposto no #37:
      # registry compilado como fronteira operacional; build:rules via TS/src (fusao
      # do ex-pr-compiler-ts ENTREGUE aqui); help derivado do registry; fallback
      # silencioso removido; AGENTS.md stub/bootstrap; monolito cercado como legacy/
      # transitorio (residuo compiler+token-budget -> CO-3). /cli agora e wrapper/
      # compatibilidade; regra governada nova vive em /src. Modo unit: NAO mergeou
      # em main (concluido na stack).
      - id: bootstrap-compiler
        github_pr: 38
        role: execution
        terminal: false
        sequence: 6
        checkpoints:
          - checkpoint-compiler-ts # motor de compilacao de regras (monolith) -> TS/src; substrato do knowledge:compile
          - checkpoint-bootstrap-registry # bootstrap/script-contracts/runtime-bootstrap -> registry/src; /cli vira wrapper+compat

      # #39 CONCLUIDO apos Human Gate npm-toolchain approved
      # (gates/c-npm-toolchain.yml, 2026-06-11; PR #39 Ready, HEAD d806a835,
      # CI 11/11 verde). Entrega aprovada: npm puro como package manager
      # canonico do framework (package-lock.json + npm ci; script-contracts/
      # hooks/CI/release/templates/docs em um unico runner; validate tambem no
      # Windows); multi-PM preservado nos consumidores (Corepack condicional);
      # FU-1 (pr-body:update preservativo) + FU-2 (pr-ready:check + sequencia
      # canonica); Template v3 + perfis de PR (PrProfileContract como SSOT);
      # review:seal; PIT-0010 graduado a GG-0004; PIT-0011 capturado; retomada
      # derivada saneada (handoff renderer sem ponteiros stale); co-projection
      # reordenado para seq 8. Ciclo de review fechado no proprio PR (TA + AR
      # approved; F1-F4 accepted via EV1). Modo unit: NAO mergeou em main
      # (concluido na stack).
      - id: toolchain-simplification
        github_pr: 39
        role: execution
        terminal: false
        sequence: 7
        checkpoints:
          - checkpoint-npm-toolchain # Yarn/Corepack -> npm puro; package-lock + npm ci; script-contracts/hooks/CI/templates/docs

      # #41 CONCLUIDO apos Human Gate co-projection approved
      # (gates/c-co-projection.yml, 2026-06-12; PR #41 Ready, HEAD 46a7ffe7,
      # CI 11/11 verde; pr-ready:check satisfeito). Entrega aprovada (CO-4):
      # handoff como ato de carga situado (deriveHandoff/deriveNextAction
      # puros; selo + recibo efemero; handoff:check advisory-first; contrato
      # global na capsula); briefing governado de reviews + review:seal/
      # review:publish + verification de dois escopos; catalogo de reviews
      # SEPARADO de obrigatoriedade (so required+nao-current bloqueia;
      # PIT-0012); security_review universal/optional (required em integration
      # por regra local); entrypoint guidelines descobrivel. 9 rodadas de
      # dogfood em research/. Reviews sob a policy nova: TA optional approved
      # (stale informativo), AR/security optional nao realizados — nenhum
      # obrigatorio. Modo unit: NAO mergeou em main (concluido na stack).
      - id: co-projection
        github_pr: 41
        role: execution
        terminal: false
        sequence: 8
        checkpoints:
          - checkpoint-co-projection # projetor situado + contrato de carga (U4); absorve ex-projection-split

      # CO-3 — Absorcao: no de conhecimento -> forma que DISPARA. EnforcementBinding
      # (declaracao->enforcement por no; PIT-0008 estrutural) + knowledge:compile
      # (reusa o compilador de regras). Absorve ex-rule-guardrail (Rule/Guardrail ->
      # Constraint + binding). (AR-2) Herda o residuo legacy-transitorio do monolito
      # cercado em checkpoint-bootstrap-registry (PR #38): compiler + token-budget +
      # rules-loader; a migracao plena para o compilador TS/src acontece aqui.
      # Candidato ao wiring do recibo de carga (assertFreshHandoffReceipt) em
      # comandos mutantes. #42 CONCLUIDO apos Human Gate co-enforcement approved
      # (gates/c-co-enforcement.yml, 2026-06-16; PR #42 Ready, HEAD 46ff89b,
      # CI 11/11 verde; pr-ready:check satisfeito). Modo unit: NAO mergeou em main
      # (concluido na stack).
      - id: co-enforcement
        github_pr: 42
        role: execution
        terminal: false
        sequence: 9
        checkpoints:
          - checkpoint-co-enforcement # EnforcementBinding + knowledge:compile (U3); absorve ex-rule-guardrail
      - id: co-flow-convergence
        github_pr: 43
        role: execution
        terminal: false
        sequence: 10
        checkpoints:
          - checkpoint-co-flow-convergence # convergencia do fluxo ponta a ponta; modelo canonico + invariantes + jornadas
      # CO-FLOW-CONTINUATION — continuacao do fluxo governado apos o recorte aprovado no PR #43.
      # PR #44 stacked sobre #43; modo unit, NAO mergeia isolado em main.
      # DEC-0024-G21 substitui CO-10.8.* como nomenclatura operacional por checkpoints
      # semanticos: drift-diagnosis-and-repair; lifecycle-model-and-artifact-taxonomy-decision;
      # artifact-taxonomy-and-model-review-contract; internal-architecture-refactor-ddd-bdd;
      # broad-flow-falsification; continuation-review-human-gate. PR #44 fecha decisao/inventario
      # e dogfood do modelo; o proximo PR implementa a taxonomia/review pre-codificacao com
      # enforcement real. dualroot-collapse continua separado para o que toca .specify.
      - id: co-flow-continuation
        github_pr: 44
        role: execution
        terminal: false
        sequence: 11
        checkpoints:
          - checkpoint-co-flow-continuation # PR #44: decisao/inventario/dogfood; continuacao segue por checkpoints semanticos definidos em DEC-0024-G21

      # #45 CONCLUIDO apos Human Gate artifact-taxonomy-and-model-review-contract approved
      # (gates/c-artifact-taxonomy-and-model-review-contract.yml, 2026-07-12; PR #45 Ready,
      # HEAD 1cc96dd1, CI verde; pr-ready:check satisfeito). Entregue: taxonomia de artefatos
      # (artifact-kind/disposition + artifact-kind:check), contrato pre-coding-review/
      # model-review, research-index:check, mapa vivo governado derivado de state.yml,
      # casa pull-requests/pr-N (DEC-0024-G26), fluxo continuation:check/prepare/create-pr
      # (DEC-0024-G27), plano situado de revisoes (review_plan; DEC-0024-G29) e extracao
      # da incubacao Guilda como evidencia historica. Modo unit: NAO mergeou em main
      # (concluido na stack). review_plan preservado como registro das decisoes situadas.
      - id: artifact-taxonomy-and-model-review-contract
        github_pr: 45
        role: execution
        terminal: false
        sequence: 12
        checkpoints:
          - checkpoint-artifact-taxonomy-and-model-review-contract # PR #45: taxonomia de artefatos + contrato de model-review/pre-coding review
        review_plan:
          technical_audit:
            system_recommendation: recommended
            owner_decision: required
            actor: owner
            reason: "PR #45 altera taxonomia, review, readiness e preservacao da incubacao Guilda; a Technical Audit foi solicitada e deve estar current+approved antes de Ready/Human Gate."
            revalidation:
              owner_decision: waived
              actor: owner
              reason: "As correcoes posteriores a TA/AR fecham cleanup low/advisory com testes e CI; nova rodada completa de TA para esse delta seria ritual, nao controle adicional. A review permanece stale na leitura historica, mas nao bloqueia Ready por decisao situada."
          architectural_review:
            system_recommendation: recommended
            owner_decision: required
            actor: owner
            reason: "PR #45 altera arquitetura de governanca, mapa vivo, casa de PR e contrato de revisao; Architectural Review deve validar a direcao antes do Human Gate."
            revalidation:
              owner_decision: waived
              actor: owner
              reason: "As correcoes posteriores a AR sao limpeza/adaptacao de findings low e nao alteram a tese arquitetural aprovada; nova AR completa para esse delta seria ritual, nao controle adicional. A review permanece stale na leitura historica, mas nao bloqueia Ready por decisao situada."
          security_review:
            system_recommendation: optional
            owner_decision: waived
            actor: owner
            reason: "Escopo atual e governanca/processo/documentacao, sem nova superficie de runtime de auth, secrets ou dados de usuario; TA/AR podem reabrir risco se encontrarem evidencia."

    active:
      # #46 e o NO TOPOLOGICO ATIVO (sequence: 13): materializa o checkpoint semantico
      # internal-architecture-refactor-ddd-bdd da continuacao (DEC-0024-G21), STACKED
      # sobre artifact-taxonomy-and-model-review-contract (#45, seq 12). Aberto pos-Human
      # Gate do #45 via continuation:create-pr --confirm (DEC-0024-G27), conforme a
      # proxima acao derivada do handoff (a Frente ainda tem checkpoints pendentes;
      # dualroot-collapse segue bloqueado ate a Frente fechar).
      # Escopo: reorganizacao behavior-preserving de src/cli e testes; gramatica
      # operacional remanescente de G01 e contrato do graph snapshot derivado
      # (DEC-0024-G23/G28), derived-only, sem 2a SSOT. Insumos obrigatorios: model.yml,
      # tracker.md, features.md e GUILDA-QRD-PRESERVATION-MATRIX.md do work-graph-model
      # (aprendizados estruturais apenas; produto Guilda permanece no repo irmao).
      - id: internal-architecture-refactor-ddd-bdd
        github_pr: 46
        role: execution
        terminal: false
        sequence: 13
        checkpoints:
          - checkpoint-internal-architecture-refactor-ddd-bdd # PR #46: refactor DDD/BDD behavior-preserving + gramatica operacional G01 + contrato do graph snapshot derivado
        # Plano situado de revisoes (DEC-0024-G29): o sistema RECOMENDA; a decisao e
        # da owner. `pending` bloqueia Ready por honestidade sem tornar nada
        # obrigatorio automaticamente — a owner grava a decisao final (com
        # actor+reason) quando escolher.
        review_plan:
          technical_audit:
            system_recommendation: recommended
            owner_decision: pending
          architectural_review:
            system_recommendation: recommended
            owner_decision: pending
          security_review:
            system_recommendation: optional
            owner_decision: pending

    planned:
      # CAUDA REESCRITA (2026-06-07, Human Gate #35 / Opcao A — reframe in-place).
      # A investigacao da 0024 expos que persistir conhecimento NAO basta: falta
      # absorcao/reconciliacao/projecao/enforcement (o humano virava o reconciliador).
      # A arquitetura de CONTINUIDADE OPERACIONAL (CO) integra a CAUDA da 0024 — NAO
      # spec paralela, NAO sucessora (evita o padrao 0025). Os ex-nos 37-41
      # (doctrine/decision/rule-guardrail/projection-split/workitem-registry) foram
      # REESCRITOS como o laco CO; a faxina ortogonal SOBREVIVE; bootstrap-compiler
      # resolve a auditoria #1/#3/#4 do #35. ELIMINADOS por INV-4 (projecao, nao
      # entidade): workitem-registry persistido como working-set (-> frontier derivada)
      # e provenance persistida (-> derivada). Ordem = IMPACTO (reduzir dependencia de
      # contexto o mais cedo). `sequence` = posicao no stack (4..K contigua). github_pr
      # null = ainda nao aberto. Objetivo/Deps de cada no: plan.md. Memoria:
      # spec-0024-continuity-operational.

      # DEC-0024-G18 — dualroot-collapse foi antecipado para antes de co-capture/co-events.
      # DEC-0024-G20 preserva essa ordem, mas posiciona a continuacao do fluxo antes do colapso.
      # A automacao de captura/eventos deve nascer contra a estrutura canonica pos-colapso,
      # nao contra a ponte .ai-guidelines/.specify ainda transitoria.
      - id: dualroot-collapse
        github_pr: null
        role: execution
        terminal: false
        sequence: 14
        checkpoints:
          - checkpoint-dualroot-collapse # colapso do dual-root; migracao .specify + .ai-guidelines (test:smoke gateia)

      # CO-5 — Fecha o laco: captura do em-transito como no na FORMACAO (PIT-0002) +
      # espinha de ciclo de vida (maturacao/graduacao human-gated). Dissolve NEXT.md
      # (conhecimento load-bearing nao vive so em prosa; INV-5). Absorve ex-decision
      # (dissolve NEXT) + ex-provenance (re-derivada como projecao, nao persistida).
      - id: co-capture
        github_pr: null
        role: execution
        terminal: false
        sequence: 15
        checkpoints:
          - checkpoint-co-capture # captura na formacao + espinha de ciclo de vida; dissolve NEXT.md (U5)

      # CO-6 — Torna o laco CONTINUO: dispatcher de eventos dispara o pipeline
      # (absorver/reconciliar/projetar/capturar) nos limites: checkpoint, commit, push,
      # merge, promocao, retomada. Repo sempre handoff-ready. Gates advisory-first.
      - id: co-events
        github_pr: null
        role: execution
        terminal: false
        sequence: 16
        checkpoints:
          - checkpoint-co-events # dispatcher de eventos / disparo automatico (U6)

      # FAXINA ortogonal a CO (SOBREVIVE da trilha de absorcao original; independe da CO).
      # Pode aterrissar em paralelo; NAO perder o ex-checkpoint-7 (CRITICO/P0).
      - id: housekeeping
        github_pr: null
        role: execution
        terminal: false
        sequence: 17
        checkpoints:
          - checkpoint-taxonomy-removal # ex-7 (CRITICO/P0): remove taxonomia evidence-driven/deterministic/mixed + ex-10 (tasks boilerplate)
          - checkpoint-agents-sync # ex-5: AGENTS sync (agents:build + agents:check)
          - checkpoint-gg-0002 # ex-6: GG-0002 banned-concept
          - checkpoint-decouple-brief # ex-9: desacopla decision-brief de evidence-driven
          - checkpoint-doc-cleanup # ex-12: limpa docs arquiteturais (workflowType)

      # Knowledge Readiness — NO FINAL antes do PR de integracao. Fecha a divida
      # planejada do CO-2.1: backfill amplo P0/P1, knowledge:health (dossie +
      # prompt de revisao assistida por IA, decisao humana), consome script-contracts
      # como input de saude operacional, e decide promocao do
      # co-knowledge:integrity/co-knowledge:check. Nao adota banco como SSOT.
      - id: knowledge-readiness
        github_pr: null
        role: execution
        terminal: false
        sequence: 18
        checkpoints:
          - checkpoint-knowledge-health # dossie de saude do grafo + candidatos semanticos p/ decisao humana
          - checkpoint-knowledge-backfill-wide # backfill amplo P0/P1 antes da integracao
          - checkpoint-co-knowledge-promotion # decide/promove checks estruturais; F4b segue humano-assistido

      - id: integration-final
        github_pr: null
        role: integration
        terminal: true
        sequence: null
        checkpoints:
          - review-and-merge
