---
node: intent
id: intent-NNN
sealed: false # a intent é VIVA — acumula aprendizado e dispara trabalhos; não sela como o brief de um trabalho
# ⚠️ "sempre explícita × emergente" em aberto: num trabalho simples a intent COLAPSA no <kind>-brief (não há esta camada)
---

# Intent — <o objetivo>

> O **objetivo durável**: dispara **N trabalhos** (de tipos variados) ao longo do tempo e se **retroalimenta** com o
> que eles aprendem. É o **coração (q→r→d) no nível do objetivo** — onde se decide qual trabalho vem a seguir.

## Kernel (o objetivo)

- **Pretendemos:** <a mudança / resultado durável que se quer no mundo>
- **Saberemos por:** <sinal de que o objetivo foi atingido>
- **Limite / fora-de-escopo:**
- **Repos tocados** (se cross-repo): <lista — é o que habilita **paralelizar** + dar **previsibilidade**>

## Trabalhos disparados (`breaks-into` — cresce com o tempo)

- `<kind>-NNN` — <o quê> — <status> _(ex.: `spike-001` → `experiment-001` → `deliv-001`)_

## Aprendizado acumulado (retroalimentação)

- de `<trabalho>` aprendemos: <…> → **próximo passo:** <…>

<!-- ⚠️ a aresta "resultado do trabalho → intent" (informs/feeds × raises/grounded-by) ainda não está fixada -->

## Estado

- <ativa | concluída | abandonada>
