node: work-brief
kind: spike
id: spike-NNN
question: # ⊛ a PERGUNTA — texto IDÊNTICO à open-question da intent (o spike-brief é a casa do conteúdo do spike)
timebox: # ⊛ prazo
references: []

# kernel — provar um ponto ANTES de entregar valor (a `question` acima é o goal)
objective:
  approach: # como investigar — PoC, benchmark, análise
  success-signal: # o que conta como resposta

unblocks-decision: # a decisão que isto destrava
output: # a RESPOSTA (não o código) → fecha em spike-answer

details: |
  <nuance opcional>
