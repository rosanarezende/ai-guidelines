node: work-brief
kind: incident
id: inc-NNN
intent: # incidente é reativo → normalmente sem intent
severity: # low | medium | high | critical
status: # mitigating | mitigated | resolved
merge-priority-until: # data
ci-bypass-until: # data (expira → sem débito, GG-0005)
references: []

# registro rápido (blameless)
symptom: # o que quebrou
impact: # impacto / afetados

closes-with: incident-postmortem # → causa-raiz + prevenção (pós-merge)

details: |
  <nuance opcional>
