# registry/patch.yml — índice (projeção) dos trabalhos kind=patch deste repo.
# 1 entrada por trabalho (schema: ../registry-entry.yml) → alimenta active-work.
entries: []
# exemplo:
#   - id: patch-001
#     title: <curto>
#     status: <draft | active | closed | done>
#     workspace: works/patch_<slug>/ # só se denso (migração/refactor grande)
#     date: <YYYY-MM-DD>
