# registry/experiment.yml — índice (projeção) dos trabalhos kind=experiment deste repo.
# 1 entrada por trabalho (schema: ../registry-entry.yml) → alimenta active-work.
entries: []
# exemplo:
#   - id: exp-001
#     title: <curto>
#     status: <draft | active | closed | done>
#     workspace: works/experiment_<slug>/ # só se denso
#     verdicted-by: <id do experiment-outcome> # quando fecha (won/lost)
#     date: <YYYY-MM-DD>
