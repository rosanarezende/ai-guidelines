stage: implementation
gate:
  status: closed
focus:
  - runtime-public-state-index
  - workflow-runtime-dogfooding
next:
  - Implementar leitura de `.governance/runtime/active-specs.yml` em `yarn workflow` (descoberta de spec ativa em `main` sem checkout manual)
  - Implementar `yarn workflow publish-state` manual + drift guard mínimo (paths existem; campos proibidos rejeitados)
  - Validar workflow ponta-a-ponta usando apenas `active-specs.yml` como descoberta primária (prova operacional real do PR3)
  - Repetir experimento de troca de máquina sem prompt humano denso
