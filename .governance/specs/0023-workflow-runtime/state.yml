stage: implementation
gate:
  status: closed
focus:
  - PR5-DX-thinking
next:
  - PR5-DX-thinking — reler Bloco B sob enforcement de PR4; Gate 3 (planning approval) explícito antes de PR6
  - PR6-DX-execution — clipboard real + AssembleBriefing warning + integration test + examples/minimal-spec + CLI help + docs/guides + README + CHANGELOG final
  - Auditoria periódica dos 4 sinais de vigilância do Passo 5 (updated_at semântica; title como fronteira; PublishState mini-orchestrator; publish-state manual-first) e dos guardrails de UX-creep no render do índice (sem stale/ordering/prioridade/próxima-spec/agregados)
